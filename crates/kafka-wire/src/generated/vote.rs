//! @generated by kafka-wire-codegen 0.1.0; do not edit.
//!
//! API key 52 from `VoteRequest.json` and `VoteResponse.json`
//! at Apache Kafka commit `678c0e07e4733c5a592e52046dc2c4e1625587f1`.
//! Request SHA-256: `aac8735bbc8f06d915dcb5659a18f75ce966f70852644b320e8ff1dd97fee9be`.
//! Response SHA-256: `3dbf931851152b179b4e96c59eb4300ae383b6bb311279a8c48af69b730ee373`.

/// `VoteRequest` and every struct it declares, under upstream's own names.
///
/// [`VoteRequest`](crate::VoteRequest) is re-exported flat, so this path never has to be
/// written to name the message itself.
pub mod vote_request {
    use kafka_wire_core::{
        ApiKey, ApiVersion, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder, KafkaDecode,
        KafkaEncode, StrBytes, TaggedFields, Uuid, VersionRange,
    };

    use crate::{KafkaMessage, KafkaRequest, RequestResponsePair};

    /// `TopicData` as declared by the `Vote` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct TopicData {
        /// The topic name.
        pub topic_name: StrBytes,
        /// The partition data.
        pub partitions: Vec<PartitionData>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl TopicData {
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 2));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl KafkaDecode for TopicData {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            let topic_name = decoder.read_compact_string()?;
            let partitions = {
                let length = decoder.read_compact_array_len()?;
                decoder.read_vec(length, |decoder| PartitionData::decode(decoder, version))?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                topic_name,
                partitions,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for TopicData {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_compact_string(&self.topic_name)?;
            encoder.write_compact_array_len(self.partitions.len())?;
            for value in &self.partitions {
                value.encode(encoder, version)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "TopicData",
                    version,
                });
            }

            Ok(())
        }
    }

    /// `PartitionData` as declared by the `Vote` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct PartitionData {
        /// The partition index.
        pub partition_index: i32,
        /// The epoch of the voter sending the request.
        pub replica_epoch: i32,
        /// The replica id of the voter sending the request.
        pub replica_id: i32,
        /// The directory id of the voter sending the request.
        pub replica_directory_id: Uuid,
        /// The directory id of the voter receiving the request.
        pub voter_directory_id: Uuid,
        /// The epoch of the last record written to the metadata log.
        pub last_offset_epoch: i32,
        /// The log end offset of the metadata log of the voter sending the request.
        pub last_offset: i64,
        /// Whether the request is a `PreVote` request (not persisted) or not.
        pub pre_vote: bool,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl PartitionData {
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 2));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl KafkaDecode for PartitionData {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            let partition_index = decoder.read_i32()?;
            let replica_epoch = decoder.read_i32()?;
            let replica_id = decoder.read_i32()?;
            let replica_directory_id = if version.value() >= 1 {
                decoder.read_uuid()?
            } else {
                Uuid::ZERO
            };
            let voter_directory_id = if version.value() >= 1 {
                decoder.read_uuid()?
            } else {
                Uuid::ZERO
            };
            let last_offset_epoch = decoder.read_i32()?;
            let last_offset = decoder.read_i64()?;
            let pre_vote = if version.value() >= 2 {
                decoder.read_bool()?
            } else {
                false
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                partition_index,
                replica_epoch,
                replica_id,
                replica_directory_id,
                voter_directory_id,
                last_offset_epoch,
                last_offset,
                pre_vote,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for PartitionData {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_i32(self.partition_index)?;
            encoder.write_i32(self.replica_epoch)?;
            encoder.write_i32(self.replica_id)?;
            if version.value() >= 1 {
                encoder.write_uuid(self.replica_directory_id)?;
            }
            if version.value() >= 1 {
                encoder.write_uuid(self.voter_directory_id)?;
            }
            encoder.write_i32(self.last_offset_epoch)?;
            encoder.write_i64(self.last_offset)?;
            if version.value() >= 2 {
                encoder.write_bool(self.pre_vote)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "PartitionData",
                    version,
                });
            }

            Ok(())
        }
    }

    /// Request body for the `Vote` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub struct VoteRequest {
        /// The cluster id.
        pub cluster_id: Option<StrBytes>,
        /// The replica id of the voter receiving the request.
        pub voter_id: i32,
        /// The topic data.
        pub topics: Vec<TopicData>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl Default for VoteRequest {
        fn default() -> Self {
            Self {
                cluster_id: None,
                voter_id: -1,
                topics: Vec::new(),
                unknown_tagged_fields: TaggedFields::default(),
            }
        }
    }

    impl KafkaMessage for VoteRequest {
        const NAME: &'static str = "VoteRequest";
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 2);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 2));
    }

    impl KafkaRequest for VoteRequest {
        const API_KEY: ApiKey = ApiKey::new(52);
    }

    impl RequestResponsePair for VoteRequest {
        type Response = super::VoteResponse;
    }

    impl KafkaDecode for VoteRequest {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            crate::message::ensure_decode_version::<Self>(version)?;

            let cluster_id = decoder.read_compact_nullable_string()?;
            let voter_id = if version.value() >= 1 {
                decoder.read_i32()?
            } else {
                -1
            };
            let topics = {
                let length = decoder.read_compact_array_len()?;
                decoder.read_vec(length, |decoder| TopicData::decode(decoder, version))?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                cluster_id,
                voter_id,
                topics,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for VoteRequest {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            crate::message::ensure_encode_version::<Self>(version)?;

            encoder.write_compact_nullable_string(self.cluster_id.as_ref())?;
            if version.value() >= 1 {
                encoder.write_i32(self.voter_id)?;
            }
            encoder.write_compact_array_len(self.topics.len())?;
            for value in &self.topics {
                value.encode(encoder, version)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: Self::NAME,
                    version,
                });
            }

            Ok(())
        }
    }
}

/// `VoteResponse` and every struct it declares, under upstream's own names.
///
/// [`VoteResponse`](crate::VoteResponse) is re-exported flat, so this path never has to be
/// written to name the message itself.
pub mod vote_response {
    use kafka_wire_core::{
        ApiKey, ApiVersion, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder, KafkaDecode,
        KafkaEncode, KnownTags, StrBytes, TagOutcome, TaggedFields, VersionRange,
    };

    use crate::{KafkaMessage, KafkaResponse};

    /// `TopicData` as declared by the `Vote` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct TopicData {
        /// The topic name.
        pub topic_name: StrBytes,
        /// The results for each partition.
        pub partitions: Vec<PartitionData>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl TopicData {
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 2));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl KafkaDecode for TopicData {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            let topic_name = decoder.read_compact_string()?;
            let partitions = {
                let length = decoder.read_compact_array_len()?;
                decoder.read_vec(length, |decoder| PartitionData::decode(decoder, version))?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                topic_name,
                partitions,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for TopicData {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_compact_string(&self.topic_name)?;
            encoder.write_compact_array_len(self.partitions.len())?;
            for value in &self.partitions {
                value.encode(encoder, version)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "TopicData",
                    version,
                });
            }

            Ok(())
        }
    }

    /// `PartitionData` as declared by the `Vote` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct PartitionData {
        /// The partition index.
        pub partition_index: i32,
        /// The partition level error code.
        pub error_code: i16,
        /// The ID of the current leader or -1 if the leader is unknown.
        pub leader_id: i32,
        /// The latest known leader epoch.
        pub leader_epoch: i32,
        /// True if the vote was granted and false otherwise.
        pub vote_granted: bool,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl PartitionData {
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 2));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl KafkaDecode for PartitionData {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            let partition_index = decoder.read_i32()?;
            let error_code = decoder.read_i16()?;
            let leader_id = decoder.read_i32()?;
            let leader_epoch = decoder.read_i32()?;
            let vote_granted = decoder.read_bool()?;
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                partition_index,
                error_code,
                leader_id,
                leader_epoch,
                vote_granted,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for PartitionData {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_i32(self.partition_index)?;
            encoder.write_i16(self.error_code)?;
            encoder.write_i32(self.leader_id)?;
            encoder.write_i32(self.leader_epoch)?;
            encoder.write_bool(self.vote_granted)?;

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "PartitionData",
                    version,
                });
            }

            Ok(())
        }
    }

    /// `NodeEndpoint` as declared by the `Vote` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct NodeEndpoint {
        /// The ID of the associated node.
        pub node_id: i32,
        /// The node's hostname.
        pub host: StrBytes,
        /// The node's port.
        pub port: u16,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl NodeEndpoint {
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 2));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl KafkaDecode for NodeEndpoint {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            let node_id = if version.value() >= 1 {
                decoder.read_i32()?
            } else {
                0
            };
            let host = if version.value() >= 1 {
                decoder.read_compact_string()?
            } else {
                StrBytes::default()
            };
            let port = if version.value() >= 1 {
                decoder.read_u16()?
            } else {
                0
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                node_id,
                host,
                port,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for NodeEndpoint {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            if version.value() >= 1 {
                encoder.write_i32(self.node_id)?;
            }
            if version.value() >= 1 {
                encoder.write_compact_string(&self.host)?;
            }
            if version.value() >= 1 {
                encoder.write_u16(self.port)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "NodeEndpoint",
                    version,
                });
            }

            Ok(())
        }
    }

    /// Response body for the `Vote` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct VoteResponse {
        /// The top level error code.
        pub error_code: i16,
        /// The results for each topic.
        pub topics: Vec<TopicData>,
        /// Endpoints for all current-leaders enumerated in `PartitionData`.
        pub node_endpoints: Vec<NodeEndpoint>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl KafkaMessage for VoteResponse {
        const NAME: &'static str = "VoteResponse";
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 2);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 2));
    }

    impl KafkaResponse for VoteResponse {
        const API_KEY: ApiKey = ApiKey::new(52);
    }

    impl KafkaDecode for VoteResponse {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            crate::message::ensure_decode_version::<Self>(version)?;

            let error_code = decoder.read_i16()?;
            let topics = {
                let length = decoder.read_compact_array_len()?;
                decoder.read_vec(length, |decoder| TopicData::decode(decoder, version))?
            };
            let mut node_endpoints: Vec<NodeEndpoint> = Vec::new();
            let mut unknown_tagged_fields = TaggedFields::default();
            if Self::is_flexible(version) {
                unknown_tagged_fields =
                    decoder.read_tagged_fields_with(|tag, decoder| match tag {
                        0 if version.value() >= 1 => {
                            node_endpoints = {
                                let length = decoder.read_compact_array_len()?;
                                decoder.read_vec(length, |decoder| {
                                    NodeEndpoint::decode(decoder, version)
                                })?
                            };
                            Ok(TagOutcome::Decoded)
                        }
                        _ => Ok(TagOutcome::Retained),
                    })?;
            }

            Ok(Self {
                error_code,
                topics,
                node_endpoints,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for VoteResponse {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            crate::message::ensure_encode_version::<Self>(version)?;

            if version.value() < 1 && !self.node_endpoints.is_empty() {
                return Err(EncodeError::FieldNotRepresentable {
                    message: Self::NAME,
                    field: "NodeEndpoints",
                    version,
                });
            }

            encoder.write_i16(self.error_code)?;
            encoder.write_compact_array_len(self.topics.len())?;
            for value in &self.topics {
                value.encode(encoder, version)?;
            }

            if Self::is_flexible(version) {
                let mut known = KnownTags::new();
                if version.value() >= 1 && !self.node_endpoints.is_empty() {
                    known.write(0, |encoder| {
                        encoder.write_compact_array_len(self.node_endpoints.len())?;
                        for value in &self.node_endpoints {
                            value.encode(encoder, version)?;
                        }
                        Ok(())
                    })?;
                }
                encoder.write_merged_tagged_fields(known, &self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: Self::NAME,
                    version,
                });
            }

            Ok(())
        }
    }
}

use kafka_wire_core::VersionRange;

use crate::{MessageDescriptor, MessageDirection};

pub use vote_request::VoteRequest;
pub use vote_response::VoteResponse;

/// Static metadata for [`VoteRequest`].
pub const VOTE_REQUEST_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    52,
    "VoteRequest",
    MessageDirection::Request,
    VersionRange::new(0, 2),
    Some(VersionRange::new(0, 2)),
);

/// Static metadata for [`VoteResponse`].
pub const VOTE_RESPONSE_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    52,
    "VoteResponse",
    MessageDirection::Response,
    VersionRange::new(0, 2),
    Some(VersionRange::new(0, 2)),
);
