//! @generated by kafka-wire-codegen 0.1.0; do not edit.
//!
//! API key 0 from `ProduceRequest.json` and `ProduceResponse.json`
//! at Apache Kafka commit `678c0e07e4733c5a592e52046dc2c4e1625587f1`.
//! Request SHA-256: `779c104bdd1753731b946384ea1fc1af08c6c6fd47f2496eadc6530a2db35966`.
//! Response SHA-256: `ea483c690ec06f83b3dcd95c5ec53f239702f754a16963d62300b156a233614a`.

/// `ProduceRequest` and every struct it declares, under upstream's own names.
///
/// [`ProduceRequest`](crate::ProduceRequest) is re-exported flat, so this path never has to be
/// written to name the message itself.
pub mod produce_request {
    use kafka_wire_core::{
        ApiKey, ApiVersion, Bytes, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder,
        KafkaDecode, KafkaEncode, StrBytes, TaggedFields, Uuid, VersionRange,
    };

    use crate::{KafkaMessage, KafkaRequest, RequestResponsePair};

    /// `TopicProduceData` as declared by the `Produce` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct TopicProduceData {
        /// The topic name.
        pub name: StrBytes,
        /// The unique topic ID.
        pub topic_id: Uuid,
        /// Each partition to produce to.
        pub partition_data: Vec<PartitionProduceData>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl TopicProduceData {
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(9, 13));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl KafkaDecode for TopicProduceData {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            let name = if version.value() <= 12 {
                if Self::is_flexible(version) {
                    decoder.read_compact_string()?
                } else {
                    decoder.read_string()?
                }
            } else {
                StrBytes::default()
            };
            let topic_id = if version.value() >= 13 {
                decoder.read_uuid()?
            } else {
                Uuid::ZERO
            };
            let partition_data = {
                let length = if Self::is_flexible(version) {
                    decoder.read_compact_array_len()?
                } else {
                    decoder.read_array_len()?
                };
                decoder.read_vec(length, |decoder| {
                    PartitionProduceData::decode(decoder, version)
                })?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                name,
                topic_id,
                partition_data,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for TopicProduceData {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            if version.value() <= 12 {
                if Self::is_flexible(version) {
                    encoder.write_compact_string(&self.name)?;
                } else {
                    encoder.write_string(&self.name)?;
                }
            }
            if version.value() >= 13 {
                encoder.write_uuid(self.topic_id)?;
            }
            if Self::is_flexible(version) {
                encoder.write_compact_array_len(self.partition_data.len())?;
            } else {
                encoder.write_array_len(self.partition_data.len())?;
            }
            for value in &self.partition_data {
                value.encode(encoder, version)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "TopicProduceData",
                    version,
                });
            }

            Ok(())
        }
    }

    /// `PartitionProduceData` as declared by the `Produce` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub struct PartitionProduceData {
        /// The partition index.
        pub index: i32,
        /// The record data to be produced.
        pub records: Option<Bytes>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl PartitionProduceData {
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(9, 13));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl Default for PartitionProduceData {
        fn default() -> Self {
            Self {
                index: 0,
                records: Some(Bytes::default()),
                unknown_tagged_fields: TaggedFields::default(),
            }
        }
    }

    impl KafkaDecode for PartitionProduceData {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            let index = decoder.read_i32()?;
            let records = if Self::is_flexible(version) {
                decoder.read_compact_nullable_bytes()?
            } else {
                decoder.read_nullable_bytes()?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                index,
                records,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for PartitionProduceData {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_i32(self.index)?;
            if Self::is_flexible(version) {
                encoder.write_compact_nullable_bytes(self.records.as_deref())?;
            } else {
                encoder.write_nullable_bytes(self.records.as_deref())?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "PartitionProduceData",
                    version,
                });
            }

            Ok(())
        }
    }

    /// Request body for the `Produce` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct ProduceRequest {
        /// The transactional ID, or null if the producer is not transactional.
        pub transactional_id: Option<StrBytes>,
        /// The number of acknowledgments the producer requires the leader to have received before considering a request complete. Allowed values: 0 for no acknowledgments, 1 for only the leader and -1 for the full ISR.
        pub acks: i16,
        /// The timeout to await a response in milliseconds.
        pub timeout_ms: i32,
        /// Each topic to produce to.
        pub topic_data: Vec<TopicProduceData>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl KafkaMessage for ProduceRequest {
        const NAME: &'static str = "ProduceRequest";
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(3, 13);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(9, 13));
    }

    impl KafkaRequest for ProduceRequest {
        const API_KEY: ApiKey = ApiKey::new(0);
    }

    impl RequestResponsePair for ProduceRequest {
        type Response = super::ProduceResponse;
    }

    impl KafkaDecode for ProduceRequest {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            crate::message::ensure_decode_version::<Self>(version)?;

            let transactional_id = if Self::is_flexible(version) {
                decoder.read_compact_nullable_string()?
            } else {
                decoder.read_nullable_string()?
            };
            let acks = decoder.read_i16()?;
            let timeout_ms = decoder.read_i32()?;
            let topic_data = {
                let length = if Self::is_flexible(version) {
                    decoder.read_compact_array_len()?
                } else {
                    decoder.read_array_len()?
                };
                decoder.read_vec(length, |decoder| TopicProduceData::decode(decoder, version))?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                transactional_id,
                acks,
                timeout_ms,
                topic_data,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for ProduceRequest {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            crate::message::ensure_encode_version::<Self>(version)?;

            if Self::is_flexible(version) {
                encoder.write_compact_nullable_string(self.transactional_id.as_ref())?;
            } else {
                encoder.write_nullable_string(self.transactional_id.as_ref())?;
            }
            encoder.write_i16(self.acks)?;
            encoder.write_i32(self.timeout_ms)?;
            if Self::is_flexible(version) {
                encoder.write_compact_array_len(self.topic_data.len())?;
            } else {
                encoder.write_array_len(self.topic_data.len())?;
            }
            for value in &self.topic_data {
                value.encode(encoder, version)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: Self::NAME,
                    version,
                });
            }

            Ok(())
        }
    }
}

/// `ProduceResponse` and every struct it declares, under upstream's own names.
///
/// [`ProduceResponse`](crate::ProduceResponse) is re-exported flat, so this path never has to be
/// written to name the message itself.
pub mod produce_response {
    use kafka_wire_core::{
        ApiKey, ApiVersion, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder, KafkaDecode,
        KafkaEncode, KnownTags, StrBytes, TagOutcome, TaggedFields, Uuid, VersionRange,
    };

    use crate::{KafkaMessage, KafkaResponse};

    /// `TopicProduceResponse` as declared by the `Produce` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct TopicProduceResponse {
        /// The topic name.
        pub name: StrBytes,
        /// The unique topic ID.
        pub topic_id: Uuid,
        /// Each partition that we produced to within the topic.
        pub partition_responses: Vec<PartitionProduceResponse>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl TopicProduceResponse {
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(9, 13));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl KafkaDecode for TopicProduceResponse {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            let name = if version.value() <= 12 {
                if Self::is_flexible(version) {
                    decoder.read_compact_string()?
                } else {
                    decoder.read_string()?
                }
            } else {
                StrBytes::default()
            };
            let topic_id = if version.value() >= 13 {
                decoder.read_uuid()?
            } else {
                Uuid::ZERO
            };
            let partition_responses = {
                let length = if Self::is_flexible(version) {
                    decoder.read_compact_array_len()?
                } else {
                    decoder.read_array_len()?
                };
                decoder.read_vec(length, |decoder| {
                    PartitionProduceResponse::decode(decoder, version)
                })?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                name,
                topic_id,
                partition_responses,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for TopicProduceResponse {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            if version.value() <= 12 {
                if Self::is_flexible(version) {
                    encoder.write_compact_string(&self.name)?;
                } else {
                    encoder.write_string(&self.name)?;
                }
            }
            if version.value() >= 13 {
                encoder.write_uuid(self.topic_id)?;
            }
            if Self::is_flexible(version) {
                encoder.write_compact_array_len(self.partition_responses.len())?;
            } else {
                encoder.write_array_len(self.partition_responses.len())?;
            }
            for value in &self.partition_responses {
                value.encode(encoder, version)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "TopicProduceResponse",
                    version,
                });
            }

            Ok(())
        }
    }

    /// `PartitionProduceResponse` as declared by the `Produce` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub struct PartitionProduceResponse {
        /// The partition index.
        pub index: i32,
        /// The error code, or 0 if there was no error.
        pub error_code: i16,
        /// The base offset.
        pub base_offset: i64,
        /// The timestamp returned by broker after appending the messages. If `CreateTime` is used for the topic, the timestamp will be -1. If `LogAppendTime` is used for the topic, the timestamp will be the broker local time when the messages are appended.
        pub log_append_time_ms: i64,
        /// The log start offset.
        pub log_start_offset: i64,
        /// The batch indices of records that caused the batch to be dropped.
        pub record_errors: Vec<BatchIndexAndErrorMessage>,
        /// The global error message summarizing the common root cause of the records that caused the batch to be dropped.
        pub error_message: Option<StrBytes>,
        /// The leader broker that the producer should use for future requests.
        pub current_leader: LeaderIdAndEpoch,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl PartitionProduceResponse {
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(9, 13));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl Default for PartitionProduceResponse {
        fn default() -> Self {
            Self {
                index: 0,
                error_code: 0,
                base_offset: 0,
                log_append_time_ms: -1,
                log_start_offset: -1,
                record_errors: Vec::new(),
                error_message: None,
                current_leader: LeaderIdAndEpoch::default(),
                unknown_tagged_fields: TaggedFields::default(),
            }
        }
    }

    impl KafkaDecode for PartitionProduceResponse {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            let index = decoder.read_i32()?;
            let error_code = decoder.read_i16()?;
            let base_offset = decoder.read_i64()?;
            let log_append_time_ms = decoder.read_i64()?;
            let log_start_offset = if version.value() >= 5 {
                decoder.read_i64()?
            } else {
                -1
            };
            let record_errors = if version.value() >= 8 {
                let length = if Self::is_flexible(version) {
                    decoder.read_compact_array_len()?
                } else {
                    decoder.read_array_len()?
                };
                decoder.read_vec(length, |decoder| {
                    BatchIndexAndErrorMessage::decode(decoder, version)
                })?
            } else {
                Vec::new()
            };
            let error_message = if version.value() >= 8 {
                if Self::is_flexible(version) {
                    decoder.read_compact_nullable_string()?
                } else {
                    decoder.read_nullable_string()?
                }
            } else {
                None
            };
            let mut current_leader: LeaderIdAndEpoch = LeaderIdAndEpoch::default();
            let mut unknown_tagged_fields = TaggedFields::default();
            if Self::is_flexible(version) {
                unknown_tagged_fields =
                    decoder.read_tagged_fields_with(|tag, decoder| match tag {
                        0 if version.value() >= 10 => {
                            current_leader = LeaderIdAndEpoch::decode(decoder, version)?;
                            Ok(TagOutcome::Decoded)
                        }
                        _ => Ok(TagOutcome::Retained),
                    })?;
            }

            Ok(Self {
                index,
                error_code,
                base_offset,
                log_append_time_ms,
                log_start_offset,
                record_errors,
                error_message,
                current_leader,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for PartitionProduceResponse {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_i32(self.index)?;
            encoder.write_i16(self.error_code)?;
            encoder.write_i64(self.base_offset)?;
            encoder.write_i64(self.log_append_time_ms)?;
            if version.value() >= 5 {
                encoder.write_i64(self.log_start_offset)?;
            }
            if version.value() >= 8 {
                if Self::is_flexible(version) {
                    encoder.write_compact_array_len(self.record_errors.len())?;
                } else {
                    encoder.write_array_len(self.record_errors.len())?;
                }
                for value in &self.record_errors {
                    value.encode(encoder, version)?;
                }
            }
            if version.value() >= 8 {
                if Self::is_flexible(version) {
                    encoder.write_compact_nullable_string(self.error_message.as_ref())?;
                } else {
                    encoder.write_nullable_string(self.error_message.as_ref())?;
                }
            }

            if Self::is_flexible(version) {
                let mut known = KnownTags::new();
                if version.value() >= 10 && self.current_leader != LeaderIdAndEpoch::default() {
                    known.write(0, |encoder| {
                        self.current_leader.encode(encoder, version)?;
                        Ok(())
                    })?;
                }
                encoder.write_merged_tagged_fields(known, &self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "PartitionProduceResponse",
                    version,
                });
            }

            Ok(())
        }
    }

    /// `BatchIndexAndErrorMessage` as declared by the `Produce` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct BatchIndexAndErrorMessage {
        /// The batch index of the record that caused the batch to be dropped.
        pub batch_index: i32,
        /// The error message of the record that caused the batch to be dropped.
        pub batch_index_error_message: Option<StrBytes>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl BatchIndexAndErrorMessage {
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(9, 13));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl KafkaDecode for BatchIndexAndErrorMessage {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            let batch_index = if version.value() >= 8 {
                decoder.read_i32()?
            } else {
                0
            };
            let batch_index_error_message = if version.value() >= 8 {
                if Self::is_flexible(version) {
                    decoder.read_compact_nullable_string()?
                } else {
                    decoder.read_nullable_string()?
                }
            } else {
                None
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                batch_index,
                batch_index_error_message,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for BatchIndexAndErrorMessage {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            if version.value() >= 8 {
                encoder.write_i32(self.batch_index)?;
            }
            if version.value() >= 8 {
                if Self::is_flexible(version) {
                    encoder
                        .write_compact_nullable_string(self.batch_index_error_message.as_ref())?;
                } else {
                    encoder.write_nullable_string(self.batch_index_error_message.as_ref())?;
                }
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "BatchIndexAndErrorMessage",
                    version,
                });
            }

            Ok(())
        }
    }

    /// `LeaderIdAndEpoch` as declared by the `Produce` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub struct LeaderIdAndEpoch {
        /// The ID of the current leader or -1 if the leader is unknown.
        pub leader_id: i32,
        /// The latest known leader epoch.
        pub leader_epoch: i32,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl LeaderIdAndEpoch {
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(9, 13));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl Default for LeaderIdAndEpoch {
        fn default() -> Self {
            Self {
                leader_id: -1,
                leader_epoch: -1,
                unknown_tagged_fields: TaggedFields::default(),
            }
        }
    }

    impl KafkaDecode for LeaderIdAndEpoch {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            let leader_id = if version.value() >= 10 {
                decoder.read_i32()?
            } else {
                -1
            };
            let leader_epoch = if version.value() >= 10 {
                decoder.read_i32()?
            } else {
                -1
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                leader_id,
                leader_epoch,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for LeaderIdAndEpoch {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            if version.value() >= 10 {
                encoder.write_i32(self.leader_id)?;
            }
            if version.value() >= 10 {
                encoder.write_i32(self.leader_epoch)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "LeaderIdAndEpoch",
                    version,
                });
            }

            Ok(())
        }
    }

    /// `NodeEndpoint` as declared by the `Produce` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct NodeEndpoint {
        /// The ID of the associated node.
        pub node_id: i32,
        /// The node's hostname.
        pub host: StrBytes,
        /// The node's port.
        pub port: i32,
        /// The rack of the node, or null if it has not been assigned to a rack.
        pub rack: Option<StrBytes>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl NodeEndpoint {
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(9, 13));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl KafkaDecode for NodeEndpoint {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            let node_id = if version.value() >= 10 {
                decoder.read_i32()?
            } else {
                0
            };
            let host = if version.value() >= 10 {
                decoder.read_compact_string()?
            } else {
                StrBytes::default()
            };
            let port = if version.value() >= 10 {
                decoder.read_i32()?
            } else {
                0
            };
            let rack = if version.value() >= 10 {
                decoder.read_compact_nullable_string()?
            } else {
                None
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                node_id,
                host,
                port,
                rack,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for NodeEndpoint {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            if version.value() >= 10 {
                encoder.write_i32(self.node_id)?;
            }
            if version.value() >= 10 {
                encoder.write_compact_string(&self.host)?;
            }
            if version.value() >= 10 {
                encoder.write_i32(self.port)?;
            }
            if version.value() >= 10 {
                encoder.write_compact_nullable_string(self.rack.as_ref())?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "NodeEndpoint",
                    version,
                });
            }

            Ok(())
        }
    }

    /// Response body for the `Produce` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct ProduceResponse {
        /// Each produce response.
        pub responses: Vec<TopicProduceResponse>,
        /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
        pub throttle_time_ms: i32,
        /// Endpoints for all current-leaders enumerated in `PartitionProduceResponses`, with errors `NOT_LEADER_OR_FOLLOWER`.
        pub node_endpoints: Vec<NodeEndpoint>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl KafkaMessage for ProduceResponse {
        const NAME: &'static str = "ProduceResponse";
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(3, 13);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(9, 13));
    }

    impl KafkaResponse for ProduceResponse {
        const API_KEY: ApiKey = ApiKey::new(0);
    }

    impl KafkaDecode for ProduceResponse {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            crate::message::ensure_decode_version::<Self>(version)?;

            let responses = {
                let length = if Self::is_flexible(version) {
                    decoder.read_compact_array_len()?
                } else {
                    decoder.read_array_len()?
                };
                decoder.read_vec(length, |decoder| {
                    TopicProduceResponse::decode(decoder, version)
                })?
            };
            let throttle_time_ms = decoder.read_i32()?;
            let mut node_endpoints: Vec<NodeEndpoint> = Vec::new();
            let mut unknown_tagged_fields = TaggedFields::default();
            if Self::is_flexible(version) {
                unknown_tagged_fields =
                    decoder.read_tagged_fields_with(|tag, decoder| match tag {
                        0 if version.value() >= 10 => {
                            node_endpoints = {
                                let length = decoder.read_compact_array_len()?;
                                decoder.read_vec(length, |decoder| {
                                    NodeEndpoint::decode(decoder, version)
                                })?
                            };
                            Ok(TagOutcome::Decoded)
                        }
                        _ => Ok(TagOutcome::Retained),
                    })?;
            }

            Ok(Self {
                responses,
                throttle_time_ms,
                node_endpoints,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for ProduceResponse {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            crate::message::ensure_encode_version::<Self>(version)?;

            if version.value() < 10 && !self.node_endpoints.is_empty() {
                return Err(EncodeError::FieldNotRepresentable {
                    message: Self::NAME,
                    field: "NodeEndpoints",
                    version,
                });
            }

            if Self::is_flexible(version) {
                encoder.write_compact_array_len(self.responses.len())?;
            } else {
                encoder.write_array_len(self.responses.len())?;
            }
            for value in &self.responses {
                value.encode(encoder, version)?;
            }
            encoder.write_i32(self.throttle_time_ms)?;

            if Self::is_flexible(version) {
                let mut known = KnownTags::new();
                if version.value() >= 10 && !self.node_endpoints.is_empty() {
                    known.write(0, |encoder| {
                        encoder.write_compact_array_len(self.node_endpoints.len())?;
                        for value in &self.node_endpoints {
                            value.encode(encoder, version)?;
                        }
                        Ok(())
                    })?;
                }
                encoder.write_merged_tagged_fields(known, &self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: Self::NAME,
                    version,
                });
            }

            Ok(())
        }
    }
}

use kafka_wire_core::VersionRange;

use crate::{MessageDescriptor, MessageDirection};

pub use produce_request::ProduceRequest;
pub use produce_response::ProduceResponse;

/// Static metadata for [`ProduceRequest`].
pub const PRODUCE_REQUEST_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    0,
    "ProduceRequest",
    MessageDirection::Request,
    VersionRange::new(3, 13),
    Some(VersionRange::new(9, 13)),
);

/// Static metadata for [`ProduceResponse`].
pub const PRODUCE_RESPONSE_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    0,
    "ProduceResponse",
    MessageDirection::Response,
    VersionRange::new(3, 13),
    Some(VersionRange::new(9, 13)),
);
