//! @generated by kafka-wire-codegen 0.1.0; do not edit.
//!
//! API key 37 from `CreatePartitionsRequest.json` and `CreatePartitionsResponse.json`
//! at Apache Kafka commit `678c0e07e4733c5a592e52046dc2c4e1625587f1`.
//! Request SHA-256: `e9b75b435d55eae3589a14330210e33fbcc9ea03af0d901a95db50ec3c1e5e26`.
//! Response SHA-256: `e48d7a78708f10c899670f556ee76f375d81f75b647b6131b7e333113f929f86`.

use kafka_wire_core::{
    ApiKey, ApiVersion, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder, KafkaDecode,
    KafkaEncode, StrBytes, TaggedFields, VersionRange,
};

use crate::{
    KafkaMessage, KafkaRequest, KafkaResponse, MessageDescriptor, MessageDirection,
    RequestResponsePair,
};

/// `CreatePartitionsRequestCreatePartitionsTopic` as declared by the `CreatePartitions` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct CreatePartitionsRequestCreatePartitionsTopic {
    /// The topic name.
    pub name: StrBytes,
    /// The new partition count.
    pub count: i32,
    /// The new partition assignments.
    pub assignments: Option<Vec<CreatePartitionsRequestCreatePartitionsAssignment>>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl CreatePartitionsRequestCreatePartitionsTopic {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(2, 3));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for CreatePartitionsRequestCreatePartitionsTopic {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let name = if Self::is_flexible(version) {
            decoder.read_compact_string()?
        } else {
            decoder.read_string()?
        };
        let count = decoder.read_i32()?;
        let assignments = {
            match if Self::is_flexible(version) {
                decoder.read_compact_nullable_array_len()?
            } else {
                decoder.read_nullable_array_len()?
            } {
                None => None,
                Some(length) => {
                    let mut values = Vec::with_capacity(length);
                    for _ in 0..length {
                        values.push(CreatePartitionsRequestCreatePartitionsAssignment::decode(
                            decoder, version,
                        )?);
                    }
                    Some(values)
                }
            }
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            name,
            count,
            assignments,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for CreatePartitionsRequestCreatePartitionsTopic {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        if Self::is_flexible(version) {
            encoder.write_compact_string(&self.name)?;
        } else {
            encoder.write_string(&self.name)?;
        }
        encoder.write_i32(self.count)?;
        if Self::is_flexible(version) {
            encoder.write_compact_nullable_array_len(self.assignments.as_ref().map(Vec::len))?;
        } else {
            encoder.write_nullable_array_len(self.assignments.as_ref().map(Vec::len))?;
        }
        if let Some(values) = &self.assignments {
            for value in values {
                value.encode(encoder, version)?;
            }
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        }

        Ok(())
    }
}

/// `CreatePartitionsRequestCreatePartitionsAssignment` as declared by the `CreatePartitions` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct CreatePartitionsRequestCreatePartitionsAssignment {
    /// The assigned broker `IDs`.
    pub broker_ids: Vec<i32>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl CreatePartitionsRequestCreatePartitionsAssignment {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(2, 3));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for CreatePartitionsRequestCreatePartitionsAssignment {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let broker_ids = {
            let length = if Self::is_flexible(version) {
                decoder.read_compact_array_len()?
            } else {
                decoder.read_array_len()?
            };
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(decoder.read_i32()?);
            }
            values
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            broker_ids,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for CreatePartitionsRequestCreatePartitionsAssignment {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        if Self::is_flexible(version) {
            encoder.write_compact_array_len(self.broker_ids.len())?;
        } else {
            encoder.write_array_len(self.broker_ids.len())?;
        }
        for value in &self.broker_ids {
            encoder.write_i32(*value)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        }

        Ok(())
    }
}

/// Request body for the `CreatePartitions` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct CreatePartitionsRequest {
    /// Each topic that we want to create new partitions inside.
    pub topics: Vec<CreatePartitionsRequestCreatePartitionsTopic>,
    /// The time in ms to wait for the partitions to be created.
    pub timeout_ms: i32,
    /// If true, then validate the request, but don't actually increase the number of partitions.
    pub validate_only: bool,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl KafkaMessage for CreatePartitionsRequest {
    const NAME: &'static str = "CreatePartitionsRequest";
    const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 3);
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(2, 3));
}

impl KafkaRequest for CreatePartitionsRequest {
    const API_KEY: ApiKey = ApiKey::new(37);
}

impl RequestResponsePair for CreatePartitionsRequest {
    type Response = CreatePartitionsResponse;
}

impl KafkaDecode for CreatePartitionsRequest {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        crate::message::ensure_decode_version::<Self>(version)?;

        let topics = {
            let length = if Self::is_flexible(version) {
                decoder.read_compact_array_len()?
            } else {
                decoder.read_array_len()?
            };
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(CreatePartitionsRequestCreatePartitionsTopic::decode(
                    decoder, version,
                )?);
            }
            values
        };
        let timeout_ms = decoder.read_i32()?;
        let validate_only = decoder.read_bool()?;
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            topics,
            timeout_ms,
            validate_only,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for CreatePartitionsRequest {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        crate::message::ensure_encode_version::<Self>(version)?;

        if Self::is_flexible(version) {
            encoder.write_compact_array_len(self.topics.len())?;
        } else {
            encoder.write_array_len(self.topics.len())?;
        }
        for value in &self.topics {
            value.encode(encoder, version)?;
        }
        encoder.write_i32(self.timeout_ms)?;
        encoder.write_bool(self.validate_only)?;

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: Self::NAME,
                version,
            });
        }

        Ok(())
    }
}

/// `CreatePartitionsResponseCreatePartitionsTopicResult` as declared by the `CreatePartitions` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct CreatePartitionsResponseCreatePartitionsTopicResult {
    /// The topic name.
    pub name: StrBytes,
    /// The result error, or zero if there was no error.
    pub error_code: i16,
    /// The result message, or null if there was no error.
    pub error_message: Option<StrBytes>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl CreatePartitionsResponseCreatePartitionsTopicResult {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(2, 3));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for CreatePartitionsResponseCreatePartitionsTopicResult {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let name = if Self::is_flexible(version) {
            decoder.read_compact_string()?
        } else {
            decoder.read_string()?
        };
        let error_code = decoder.read_i16()?;
        let error_message = if Self::is_flexible(version) {
            decoder.read_compact_nullable_string()?
        } else {
            decoder.read_nullable_string()?
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            name,
            error_code,
            error_message,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for CreatePartitionsResponseCreatePartitionsTopicResult {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        if Self::is_flexible(version) {
            encoder.write_compact_string(&self.name)?;
        } else {
            encoder.write_string(&self.name)?;
        }
        encoder.write_i16(self.error_code)?;
        if Self::is_flexible(version) {
            encoder.write_compact_nullable_string(self.error_message.as_ref())?;
        } else {
            encoder.write_nullable_string(self.error_message.as_ref())?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        }

        Ok(())
    }
}

/// Response body for the `CreatePartitions` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct CreatePartitionsResponse {
    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    pub throttle_time_ms: i32,
    /// The partition creation results for each topic.
    pub results: Vec<CreatePartitionsResponseCreatePartitionsTopicResult>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl KafkaMessage for CreatePartitionsResponse {
    const NAME: &'static str = "CreatePartitionsResponse";
    const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 3);
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(2, 3));
}

impl KafkaResponse for CreatePartitionsResponse {
    const API_KEY: ApiKey = ApiKey::new(37);
}

impl KafkaDecode for CreatePartitionsResponse {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        crate::message::ensure_decode_version::<Self>(version)?;

        let throttle_time_ms = decoder.read_i32()?;
        let results = {
            let length = if Self::is_flexible(version) {
                decoder.read_compact_array_len()?
            } else {
                decoder.read_array_len()?
            };
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(CreatePartitionsResponseCreatePartitionsTopicResult::decode(
                    decoder, version,
                )?);
            }
            values
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            throttle_time_ms,
            results,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for CreatePartitionsResponse {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        crate::message::ensure_encode_version::<Self>(version)?;

        encoder.write_i32(self.throttle_time_ms)?;
        if Self::is_flexible(version) {
            encoder.write_compact_array_len(self.results.len())?;
        } else {
            encoder.write_array_len(self.results.len())?;
        }
        for value in &self.results {
            value.encode(encoder, version)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: Self::NAME,
                version,
            });
        }

        Ok(())
    }
}

/// Static metadata for [`CreatePartitionsRequest`].
pub const CREATE_PARTITIONS_REQUEST_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    37,
    "CreatePartitionsRequest",
    MessageDirection::Request,
    VersionRange::new(0, 3),
    Some(VersionRange::new(2, 3)),
);

/// Static metadata for [`CreatePartitionsResponse`].
pub const CREATE_PARTITIONS_RESPONSE_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    37,
    "CreatePartitionsResponse",
    MessageDirection::Response,
    VersionRange::new(0, 3),
    Some(VersionRange::new(2, 3)),
);
