//! @generated by kafka-wire-codegen 0.1.0; do not edit.
//!
//! API key 54 from `EndQuorumEpochRequest.json` and `EndQuorumEpochResponse.json`
//! at Apache Kafka commit `678c0e07e4733c5a592e52046dc2c4e1625587f1`.
//! Request SHA-256: `3f23f77fce1f1f8b062152071919955004aa81aeb31ecabd37fd9dc73d86f649`.
//! Response SHA-256: `1769e13f2a2ccabe50bb0c4d6fdd73de9ffa97bfee765d0c926687146ed8d75f`.

use kafka_wire_core::{
    ApiKey, ApiVersion, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder, KafkaDecode,
    KafkaEncode, KnownTags, StrBytes, TagOutcome, TaggedFields, Uuid, VersionRange,
};

use crate::{
    KafkaMessage, KafkaRequest, KafkaResponse, MessageDescriptor, MessageDirection,
    RequestResponsePair,
};

/// `EndQuorumEpochRequestTopicData` as declared by the `EndQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct EndQuorumEpochRequestTopicData {
    /// The topic name.
    pub topic_name: StrBytes,
    /// The partitions.
    pub partitions: Vec<EndQuorumEpochRequestPartitionData>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl EndQuorumEpochRequestTopicData {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for EndQuorumEpochRequestTopicData {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let topic_name = if Self::is_flexible(version) {
            decoder.read_compact_string()?
        } else {
            decoder.read_string()?
        };
        let partitions = {
            let length = if Self::is_flexible(version) {
                decoder.read_compact_array_len()?
            } else {
                decoder.read_array_len()?
            };
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(EndQuorumEpochRequestPartitionData::decode(
                    decoder, version,
                )?);
            }
            values
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            topic_name,
            partitions,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for EndQuorumEpochRequestTopicData {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        if Self::is_flexible(version) {
            encoder.write_compact_string(&self.topic_name)?;
        } else {
            encoder.write_string(&self.topic_name)?;
        }
        if Self::is_flexible(version) {
            encoder.write_compact_array_len(self.partitions.len())?;
        } else {
            encoder.write_array_len(self.partitions.len())?;
        }
        for value in &self.partitions {
            value.encode(encoder, version)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "EndQuorumEpochRequestTopicData",
                version,
            });
        }

        Ok(())
    }
}

/// `EndQuorumEpochRequestPartitionData` as declared by the `EndQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct EndQuorumEpochRequestPartitionData {
    /// The partition index.
    pub partition_index: i32,
    /// The current leader ID that is resigning.
    pub leader_id: i32,
    /// The current epoch.
    pub leader_epoch: i32,
    /// A sorted list of preferred successors to start the election.
    pub preferred_successors: Vec<i32>,
    /// A sorted list of preferred candidates to start the election.
    pub preferred_candidates: Vec<EndQuorumEpochRequestReplicaInfo>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl EndQuorumEpochRequestPartitionData {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for EndQuorumEpochRequestPartitionData {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let partition_index = decoder.read_i32()?;
        let leader_id = decoder.read_i32()?;
        let leader_epoch = decoder.read_i32()?;
        let preferred_successors = if version.value() <= 0 {
            let length = decoder.read_array_len()?;
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(decoder.read_i32()?);
            }
            values
        } else {
            Vec::new()
        };
        let preferred_candidates = if version.value() >= 1 {
            let length = decoder.read_compact_array_len()?;
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(EndQuorumEpochRequestReplicaInfo::decode(decoder, version)?);
            }
            values
        } else {
            Vec::new()
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            partition_index,
            leader_id,
            leader_epoch,
            preferred_successors,
            preferred_candidates,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for EndQuorumEpochRequestPartitionData {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        encoder.write_i32(self.partition_index)?;
        encoder.write_i32(self.leader_id)?;
        encoder.write_i32(self.leader_epoch)?;
        if version.value() <= 0 {
            encoder.write_array_len(self.preferred_successors.len())?;
            for value in &self.preferred_successors {
                encoder.write_i32(*value)?;
            }
        }
        if version.value() >= 1 {
            encoder.write_compact_array_len(self.preferred_candidates.len())?;
            for value in &self.preferred_candidates {
                value.encode(encoder, version)?;
            }
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "EndQuorumEpochRequestPartitionData",
                version,
            });
        }

        Ok(())
    }
}

/// `EndQuorumEpochRequestReplicaInfo` as declared by the `EndQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct EndQuorumEpochRequestReplicaInfo {
    /// The ID of the candidate replica.
    pub candidate_id: i32,
    /// The directory ID of the candidate replica.
    pub candidate_directory_id: Uuid,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl EndQuorumEpochRequestReplicaInfo {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for EndQuorumEpochRequestReplicaInfo {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let candidate_id = if version.value() >= 1 {
            decoder.read_i32()?
        } else {
            0
        };
        let candidate_directory_id = if version.value() >= 1 {
            decoder.read_uuid()?
        } else {
            Uuid::ZERO
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            candidate_id,
            candidate_directory_id,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for EndQuorumEpochRequestReplicaInfo {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        if version.value() >= 1 {
            encoder.write_i32(self.candidate_id)?;
        }
        if version.value() >= 1 {
            encoder.write_uuid(self.candidate_directory_id)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "EndQuorumEpochRequestReplicaInfo",
                version,
            });
        }

        Ok(())
    }
}

/// `EndQuorumEpochRequestLeaderEndpoint` as declared by the `EndQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct EndQuorumEpochRequestLeaderEndpoint {
    /// The name of the endpoint.
    pub name: StrBytes,
    /// The node's hostname.
    pub host: StrBytes,
    /// The node's port.
    pub port: u16,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl EndQuorumEpochRequestLeaderEndpoint {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for EndQuorumEpochRequestLeaderEndpoint {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let name = if version.value() >= 1 {
            decoder.read_compact_string()?
        } else {
            StrBytes::default()
        };
        let host = if version.value() >= 1 {
            decoder.read_compact_string()?
        } else {
            StrBytes::default()
        };
        let port = if version.value() >= 1 {
            decoder.read_u16()?
        } else {
            0
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            name,
            host,
            port,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for EndQuorumEpochRequestLeaderEndpoint {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        if version.value() >= 1 {
            encoder.write_compact_string(&self.name)?;
        }
        if version.value() >= 1 {
            encoder.write_compact_string(&self.host)?;
        }
        if version.value() >= 1 {
            encoder.write_u16(self.port)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "EndQuorumEpochRequestLeaderEndpoint",
                version,
            });
        }

        Ok(())
    }
}

/// Request body for the `EndQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct EndQuorumEpochRequest {
    /// The cluster id.
    pub cluster_id: Option<StrBytes>,
    /// The topics.
    pub topics: Vec<EndQuorumEpochRequestTopicData>,
    /// Endpoints for the leader.
    pub leader_endpoints: Vec<EndQuorumEpochRequestLeaderEndpoint>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl KafkaMessage for EndQuorumEpochRequest {
    const NAME: &'static str = "EndQuorumEpochRequest";
    const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 1);
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));
}

impl KafkaRequest for EndQuorumEpochRequest {
    const API_KEY: ApiKey = ApiKey::new(54);
}

impl RequestResponsePair for EndQuorumEpochRequest {
    type Response = EndQuorumEpochResponse;
}

impl KafkaDecode for EndQuorumEpochRequest {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        crate::message::ensure_decode_version::<Self>(version)?;

        let cluster_id = if Self::is_flexible(version) {
            decoder.read_compact_nullable_string()?
        } else {
            decoder.read_nullable_string()?
        };
        let topics = {
            let length = if Self::is_flexible(version) {
                decoder.read_compact_array_len()?
            } else {
                decoder.read_array_len()?
            };
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(EndQuorumEpochRequestTopicData::decode(decoder, version)?);
            }
            values
        };
        let leader_endpoints = if version.value() >= 1 {
            let length = decoder.read_compact_array_len()?;
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(EndQuorumEpochRequestLeaderEndpoint::decode(
                    decoder, version,
                )?);
            }
            values
        } else {
            Vec::new()
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            cluster_id,
            topics,
            leader_endpoints,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for EndQuorumEpochRequest {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        crate::message::ensure_encode_version::<Self>(version)?;

        if Self::is_flexible(version) {
            encoder.write_compact_nullable_string(self.cluster_id.as_ref())?;
        } else {
            encoder.write_nullable_string(self.cluster_id.as_ref())?;
        }
        if Self::is_flexible(version) {
            encoder.write_compact_array_len(self.topics.len())?;
        } else {
            encoder.write_array_len(self.topics.len())?;
        }
        for value in &self.topics {
            value.encode(encoder, version)?;
        }
        if version.value() >= 1 {
            encoder.write_compact_array_len(self.leader_endpoints.len())?;
            for value in &self.leader_endpoints {
                value.encode(encoder, version)?;
            }
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: Self::NAME,
                version,
            });
        }

        Ok(())
    }
}

/// `EndQuorumEpochResponseTopicData` as declared by the `EndQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct EndQuorumEpochResponseTopicData {
    /// The topic name.
    pub topic_name: StrBytes,
    /// The partition data.
    pub partitions: Vec<EndQuorumEpochResponsePartitionData>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl EndQuorumEpochResponseTopicData {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for EndQuorumEpochResponseTopicData {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let topic_name = if Self::is_flexible(version) {
            decoder.read_compact_string()?
        } else {
            decoder.read_string()?
        };
        let partitions = {
            let length = if Self::is_flexible(version) {
                decoder.read_compact_array_len()?
            } else {
                decoder.read_array_len()?
            };
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(EndQuorumEpochResponsePartitionData::decode(
                    decoder, version,
                )?);
            }
            values
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            topic_name,
            partitions,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for EndQuorumEpochResponseTopicData {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        if Self::is_flexible(version) {
            encoder.write_compact_string(&self.topic_name)?;
        } else {
            encoder.write_string(&self.topic_name)?;
        }
        if Self::is_flexible(version) {
            encoder.write_compact_array_len(self.partitions.len())?;
        } else {
            encoder.write_array_len(self.partitions.len())?;
        }
        for value in &self.partitions {
            value.encode(encoder, version)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "EndQuorumEpochResponseTopicData",
                version,
            });
        }

        Ok(())
    }
}

/// `EndQuorumEpochResponsePartitionData` as declared by the `EndQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct EndQuorumEpochResponsePartitionData {
    /// The partition index.
    pub partition_index: i32,
    /// The partition level error code.
    pub error_code: i16,
    /// The ID of the current leader or -1 if the leader is unknown.
    pub leader_id: i32,
    /// The latest known leader epoch.
    pub leader_epoch: i32,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl EndQuorumEpochResponsePartitionData {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for EndQuorumEpochResponsePartitionData {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let partition_index = decoder.read_i32()?;
        let error_code = decoder.read_i16()?;
        let leader_id = decoder.read_i32()?;
        let leader_epoch = decoder.read_i32()?;
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            partition_index,
            error_code,
            leader_id,
            leader_epoch,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for EndQuorumEpochResponsePartitionData {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        encoder.write_i32(self.partition_index)?;
        encoder.write_i16(self.error_code)?;
        encoder.write_i32(self.leader_id)?;
        encoder.write_i32(self.leader_epoch)?;

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "EndQuorumEpochResponsePartitionData",
                version,
            });
        }

        Ok(())
    }
}

/// `EndQuorumEpochResponseNodeEndpoint` as declared by the `EndQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct EndQuorumEpochResponseNodeEndpoint {
    /// The ID of the associated node.
    pub node_id: i32,
    /// The node's hostname.
    pub host: StrBytes,
    /// The node's port.
    pub port: u16,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl EndQuorumEpochResponseNodeEndpoint {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for EndQuorumEpochResponseNodeEndpoint {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let node_id = if version.value() >= 1 {
            decoder.read_i32()?
        } else {
            0
        };
        let host = if version.value() >= 1 {
            decoder.read_compact_string()?
        } else {
            StrBytes::default()
        };
        let port = if version.value() >= 1 {
            decoder.read_u16()?
        } else {
            0
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            node_id,
            host,
            port,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for EndQuorumEpochResponseNodeEndpoint {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        if version.value() >= 1 {
            encoder.write_i32(self.node_id)?;
        }
        if version.value() >= 1 {
            encoder.write_compact_string(&self.host)?;
        }
        if version.value() >= 1 {
            encoder.write_u16(self.port)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "EndQuorumEpochResponseNodeEndpoint",
                version,
            });
        }

        Ok(())
    }
}

/// Response body for the `EndQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct EndQuorumEpochResponse {
    /// The top level error code.
    pub error_code: i16,
    /// The topic data.
    pub topics: Vec<EndQuorumEpochResponseTopicData>,
    /// Endpoints for all leaders enumerated in `PartitionData`.
    pub node_endpoints: Vec<EndQuorumEpochResponseNodeEndpoint>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl KafkaMessage for EndQuorumEpochResponse {
    const NAME: &'static str = "EndQuorumEpochResponse";
    const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 1);
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));
}

impl KafkaResponse for EndQuorumEpochResponse {
    const API_KEY: ApiKey = ApiKey::new(54);
}

impl KafkaDecode for EndQuorumEpochResponse {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        crate::message::ensure_decode_version::<Self>(version)?;

        let error_code = decoder.read_i16()?;
        let topics = {
            let length = if Self::is_flexible(version) {
                decoder.read_compact_array_len()?
            } else {
                decoder.read_array_len()?
            };
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(EndQuorumEpochResponseTopicData::decode(decoder, version)?);
            }
            values
        };
        let mut node_endpoints: Vec<EndQuorumEpochResponseNodeEndpoint> = Vec::new();
        let mut unknown_tagged_fields = TaggedFields::default();
        if Self::is_flexible(version) {
            unknown_tagged_fields = decoder.read_tagged_fields_with(|tag, decoder| match tag {
                0 => {
                    node_endpoints = {
                        let length = decoder.read_compact_array_len()?;
                        let mut values = Vec::with_capacity(length);
                        for _ in 0..length {
                            values.push(EndQuorumEpochResponseNodeEndpoint::decode(
                                decoder, version,
                            )?);
                        }
                        values
                    };
                    Ok(TagOutcome::Decoded)
                }
                _ => Ok(TagOutcome::Retained),
            })?;
        }

        Ok(Self {
            error_code,
            topics,
            node_endpoints,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for EndQuorumEpochResponse {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        crate::message::ensure_encode_version::<Self>(version)?;

        if version.value() < 1 && !self.node_endpoints.is_empty() {
            return Err(EncodeError::FieldNotRepresentable {
                message: Self::NAME,
                field: "NodeEndpoints",
                version,
            });
        }

        encoder.write_i16(self.error_code)?;
        if Self::is_flexible(version) {
            encoder.write_compact_array_len(self.topics.len())?;
        } else {
            encoder.write_array_len(self.topics.len())?;
        }
        for value in &self.topics {
            value.encode(encoder, version)?;
        }

        if Self::is_flexible(version) {
            let mut known = KnownTags::new();
            if !self.node_endpoints.is_empty() {
                known.write(0, |encoder| {
                    encoder.write_compact_array_len(self.node_endpoints.len())?;
                    for value in &self.node_endpoints {
                        value.encode(encoder, version)?;
                    }
                    Ok(())
                })?;
            }
            encoder.write_merged_tagged_fields(known, &self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: Self::NAME,
                version,
            });
        }

        Ok(())
    }
}

/// Static metadata for [`EndQuorumEpochRequest`].
pub const END_QUORUM_EPOCH_REQUEST_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    54,
    "EndQuorumEpochRequest",
    MessageDirection::Request,
    VersionRange::new(0, 1),
    Some(VersionRange::new(1, 1)),
);

/// Static metadata for [`EndQuorumEpochResponse`].
pub const END_QUORUM_EPOCH_RESPONSE_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    54,
    "EndQuorumEpochResponse",
    MessageDirection::Response,
    VersionRange::new(0, 1),
    Some(VersionRange::new(1, 1)),
);
