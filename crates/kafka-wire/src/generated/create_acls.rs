//! @generated by kafka-wire-codegen 0.1.0; do not edit.
//!
//! API key 30 from `CreateAclsRequest.json` and `CreateAclsResponse.json`
//! at Apache Kafka commit `678c0e07e4733c5a592e52046dc2c4e1625587f1`.
//! Request SHA-256: `75add36b48154d659ee614b594d7826cbfbf897771e96239026f1d969f167e42`.
//! Response SHA-256: `278d972a9379ea10be5a617bcaeedcfeea064da28d5fcd7b7f98dee13fd9242e`.

/// `CreateAclsRequest` and every struct it declares, under upstream's own names.
///
/// [`CreateAclsRequest`](crate::CreateAclsRequest) is re-exported flat, so this path never has to be
/// written to name the message itself.
pub mod create_acls_request {
    use kafka_wire_core::{
        ApiKey, ApiVersion, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder, KafkaDecode,
        KafkaEncode, StrBytes, TaggedFields, VersionRange,
    };

    use crate::{KafkaMessage, KafkaRequest, RequestResponsePair};

    /// `AclCreation` as declared by the `CreateAcls` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub struct AclCreation {
        /// The type of the resource.
        pub resource_type: i8,
        /// The resource name for the ACL.
        pub resource_name: StrBytes,
        /// The pattern type for the ACL.
        pub resource_pattern_type: i8,
        /// The principal for the ACL.
        pub principal: StrBytes,
        /// The host for the ACL.
        pub host: StrBytes,
        /// The operation type for the ACL (read, write, etc.).
        pub operation: i8,
        /// The permission type for the ACL (allow, deny, etc.).
        pub permission_type: i8,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl AclCreation {
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(1, 3);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(2, 3));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl AclCreation {
        fn validate_for_version(&self, version: ApiVersion) -> Result<(), EncodeError> {
            if !Self::SUPPORTED_VERSIONS.contains(version) {
                return Err(EncodeError::UnsupportedVersion {
                    message: "AclCreation",
                    version,
                    supported: Self::SUPPORTED_VERSIONS,
                });
            }

            if !Self::is_flexible(version) && !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "AclCreation",
                    version,
                });
            }

            Ok(())
        }
    }

    impl Default for AclCreation {
        fn default() -> Self {
            Self {
                resource_type: 0,
                resource_name: StrBytes::default(),
                resource_pattern_type: 3,
                principal: StrBytes::default(),
                host: StrBytes::default(),
                operation: 0,
                permission_type: 0,
                unknown_tagged_fields: TaggedFields::default(),
            }
        }
    }

    impl KafkaDecode for AclCreation {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            if !Self::SUPPORTED_VERSIONS.contains(version) {
                return Err(DecodeError::UnsupportedVersion {
                    message: "AclCreation",
                    version,
                    supported: Self::SUPPORTED_VERSIONS,
                });
            }

            let resource_type = decoder.read_i8()?;
            let resource_name = if Self::is_flexible(version) {
                decoder.read_compact_string()?
            } else {
                decoder.read_string()?
            };
            let resource_pattern_type = decoder.read_i8()?;
            let principal = if Self::is_flexible(version) {
                decoder.read_compact_string()?
            } else {
                decoder.read_string()?
            };
            let host = if Self::is_flexible(version) {
                decoder.read_compact_string()?
            } else {
                decoder.read_string()?
            };
            let operation = decoder.read_i8()?;
            let permission_type = decoder.read_i8()?;
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                resource_type,
                resource_name,
                resource_pattern_type,
                principal,
                host,
                operation,
                permission_type,
                unknown_tagged_fields,
            })
        }
    }

    impl AclCreation {
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_i8(self.resource_type)?;
            if Self::is_flexible(version) {
                encoder.write_compact_string(&self.resource_name)?;
            } else {
                encoder.write_string(&self.resource_name)?;
            }
            encoder.write_i8(self.resource_pattern_type)?;
            if Self::is_flexible(version) {
                encoder.write_compact_string(&self.principal)?;
            } else {
                encoder.write_string(&self.principal)?;
            }
            if Self::is_flexible(version) {
                encoder.write_compact_string(&self.host)?;
            } else {
                encoder.write_string(&self.host)?;
            }
            encoder.write_i8(self.operation)?;
            encoder.write_i8(self.permission_type)?;

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            }

            Ok(())
        }
    }

    impl KafkaEncode for AclCreation {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            self.validate_for_version(version)?;
            AclCreation::encode_validated(self, encoder, version)
        }

        #[doc(hidden)]
        fn validate_encoding(&self, version: ApiVersion) -> Result<(), EncodeError> {
            self.validate_for_version(version)
        }

        #[doc(hidden)]
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            AclCreation::encode_validated(self, encoder, version)
        }
    }

    /// Request body for the `CreateAcls` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct CreateAclsRequest {
        /// The `ACLs` that we want to create.
        pub creations: Vec<AclCreation>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl KafkaMessage for CreateAclsRequest {
        const NAME: &'static str = "CreateAclsRequest";
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(1, 3);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(2, 3));
    }

    impl KafkaRequest for CreateAclsRequest {
        const API_KEY: ApiKey = ApiKey::new(30);
    }

    impl RequestResponsePair for CreateAclsRequest {
        type Response = super::CreateAclsResponse;
    }

    impl CreateAclsRequest {
        fn validate_for_version(&self, version: ApiVersion) -> Result<(), EncodeError> {
            crate::message::ensure_encode_version::<Self>(version)?;

            for value in &self.creations {
                value.validate_for_version(version)?;
            }
            if !Self::is_flexible(version) && !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: Self::NAME,
                    version,
                });
            }

            Ok(())
        }
    }

    impl KafkaDecode for CreateAclsRequest {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            crate::message::ensure_decode_version::<Self>(version)?;

            let creations = {
                let length = if Self::is_flexible(version) {
                    decoder.read_compact_array_len()?
                } else {
                    decoder.read_array_len()?
                };
                decoder.read_vec(length, |decoder| AclCreation::decode(decoder, version))?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                creations,
                unknown_tagged_fields,
            })
        }
    }

    impl CreateAclsRequest {
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            if Self::is_flexible(version) {
                encoder.write_compact_array_len(self.creations.len())?;
            } else {
                encoder.write_array_len(self.creations.len())?;
            }
            for value in &self.creations {
                value.encode_validated(encoder, version)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            }

            Ok(())
        }
    }

    impl KafkaEncode for CreateAclsRequest {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            self.validate_for_version(version)?;
            CreateAclsRequest::encode_validated(self, encoder, version)
        }

        #[doc(hidden)]
        fn validate_encoding(&self, version: ApiVersion) -> Result<(), EncodeError> {
            self.validate_for_version(version)
        }

        #[doc(hidden)]
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            CreateAclsRequest::encode_validated(self, encoder, version)
        }
    }
}

/// `CreateAclsResponse` and every struct it declares, under upstream's own names.
///
/// [`CreateAclsResponse`](crate::CreateAclsResponse) is re-exported flat, so this path never has to be
/// written to name the message itself.
pub mod create_acls_response {
    use kafka_wire_core::{
        ApiKey, ApiVersion, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder, KafkaDecode,
        KafkaEncode, StrBytes, TaggedFields, VersionRange,
    };

    use crate::{KafkaMessage, KafkaResponse};

    /// `AclCreationResult` as declared by the `CreateAcls` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub struct AclCreationResult {
        /// The result error, or zero if there was no error.
        pub error_code: i16,
        /// The result message, or null if there was no error.
        pub error_message: Option<StrBytes>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl AclCreationResult {
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(1, 3);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(2, 3));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl AclCreationResult {
        fn validate_for_version(&self, version: ApiVersion) -> Result<(), EncodeError> {
            if !Self::SUPPORTED_VERSIONS.contains(version) {
                return Err(EncodeError::UnsupportedVersion {
                    message: "AclCreationResult",
                    version,
                    supported: Self::SUPPORTED_VERSIONS,
                });
            }

            if !Self::is_flexible(version) && !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "AclCreationResult",
                    version,
                });
            }

            Ok(())
        }
    }

    impl Default for AclCreationResult {
        fn default() -> Self {
            Self {
                error_code: 0,
                error_message: Some(StrBytes::default()),
                unknown_tagged_fields: TaggedFields::default(),
            }
        }
    }

    impl KafkaDecode for AclCreationResult {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            if !Self::SUPPORTED_VERSIONS.contains(version) {
                return Err(DecodeError::UnsupportedVersion {
                    message: "AclCreationResult",
                    version,
                    supported: Self::SUPPORTED_VERSIONS,
                });
            }

            let error_code = decoder.read_i16()?;
            let error_message = if Self::is_flexible(version) {
                decoder.read_compact_nullable_string()?
            } else {
                decoder.read_nullable_string()?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                error_code,
                error_message,
                unknown_tagged_fields,
            })
        }
    }

    impl AclCreationResult {
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_i16(self.error_code)?;
            if Self::is_flexible(version) {
                encoder.write_compact_nullable_string(self.error_message.as_ref())?;
            } else {
                encoder.write_nullable_string(self.error_message.as_ref())?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            }

            Ok(())
        }
    }

    impl KafkaEncode for AclCreationResult {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            self.validate_for_version(version)?;
            AclCreationResult::encode_validated(self, encoder, version)
        }

        #[doc(hidden)]
        fn validate_encoding(&self, version: ApiVersion) -> Result<(), EncodeError> {
            self.validate_for_version(version)
        }

        #[doc(hidden)]
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            AclCreationResult::encode_validated(self, encoder, version)
        }
    }

    /// Response body for the `CreateAcls` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct CreateAclsResponse {
        /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
        pub throttle_time_ms: i32,
        /// The results for each ACL creation.
        pub results: Vec<AclCreationResult>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl KafkaMessage for CreateAclsResponse {
        const NAME: &'static str = "CreateAclsResponse";
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(1, 3);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(2, 3));
    }

    impl KafkaResponse for CreateAclsResponse {
        const API_KEY: ApiKey = ApiKey::new(30);
    }

    impl CreateAclsResponse {
        fn validate_for_version(&self, version: ApiVersion) -> Result<(), EncodeError> {
            crate::message::ensure_encode_version::<Self>(version)?;

            for value in &self.results {
                value.validate_for_version(version)?;
            }
            if !Self::is_flexible(version) && !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: Self::NAME,
                    version,
                });
            }

            Ok(())
        }
    }

    impl KafkaDecode for CreateAclsResponse {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            crate::message::ensure_decode_version::<Self>(version)?;

            let throttle_time_ms = decoder.read_i32()?;
            let results = {
                let length = if Self::is_flexible(version) {
                    decoder.read_compact_array_len()?
                } else {
                    decoder.read_array_len()?
                };
                decoder.read_vec(length, |decoder| {
                    AclCreationResult::decode(decoder, version)
                })?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                throttle_time_ms,
                results,
                unknown_tagged_fields,
            })
        }
    }

    impl CreateAclsResponse {
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_i32(self.throttle_time_ms)?;
            if Self::is_flexible(version) {
                encoder.write_compact_array_len(self.results.len())?;
            } else {
                encoder.write_array_len(self.results.len())?;
            }
            for value in &self.results {
                value.encode_validated(encoder, version)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            }

            Ok(())
        }
    }

    impl KafkaEncode for CreateAclsResponse {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            self.validate_for_version(version)?;
            CreateAclsResponse::encode_validated(self, encoder, version)
        }

        #[doc(hidden)]
        fn validate_encoding(&self, version: ApiVersion) -> Result<(), EncodeError> {
            self.validate_for_version(version)
        }

        #[doc(hidden)]
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            CreateAclsResponse::encode_validated(self, encoder, version)
        }
    }
}

use kafka_wire_core::VersionRange;

use crate::{MessageDescriptor, MessageDirection};

pub use create_acls_request::CreateAclsRequest;
pub use create_acls_response::CreateAclsResponse;

/// Static metadata for [`CreateAclsRequest`].
pub const CREATE_ACLS_REQUEST_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    30,
    "CreateAclsRequest",
    MessageDirection::Request,
    VersionRange::new(1, 3),
    Some(VersionRange::new(2, 3)),
);

/// Static metadata for [`CreateAclsResponse`].
pub const CREATE_ACLS_RESPONSE_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    30,
    "CreateAclsResponse",
    MessageDirection::Response,
    VersionRange::new(1, 3),
    Some(VersionRange::new(2, 3)),
);
