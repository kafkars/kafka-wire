//! @generated by kafka-wire-codegen 0.1.0; do not edit.
//!
//! API key 58 from `EnvelopeRequest.json` and `EnvelopeResponse.json`
//! at Apache Kafka commit `678c0e07e4733c5a592e52046dc2c4e1625587f1`.
//! Request SHA-256: `9f94021888237eb8099ddda04f5c4ed797769a39bd06479d611827b1d6dcf723`.
//! Response SHA-256: `0553772a2b4c033a48f2c2a7848d2d8947818235ab954190f55c40fb6fe9df38`.

/// `EnvelopeRequest` and every struct it declares, under upstream's own names.
///
/// [`EnvelopeRequest`](crate::EnvelopeRequest) is re-exported flat, so this path never has to be
/// written to name the message itself.
pub mod envelope_request {
    use kafka_wire_core::{
        ApiKey, ApiVersion, Bytes, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder,
        KafkaDecode, KafkaEncode, TaggedFields, VersionRange,
    };

    use crate::{KafkaMessage, KafkaRequest, RequestResponsePair};

    /// Request body for the `Envelope` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub struct EnvelopeRequest {
        /// The embedded request header and data.
        pub request_data: Bytes,
        /// Value of the initial client principal when the request is redirected by a broker.
        pub request_principal: Option<Bytes>,
        /// The original client's address in bytes.
        pub client_host_address: Bytes,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl Default for EnvelopeRequest {
        fn default() -> Self {
            Self {
                request_data: Bytes::default(),
                request_principal: Some(Bytes::default()),
                client_host_address: Bytes::default(),
                unknown_tagged_fields: TaggedFields::default(),
            }
        }
    }

    impl KafkaMessage for EnvelopeRequest {
        const NAME: &'static str = "EnvelopeRequest";
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 0);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));
    }

    impl KafkaRequest for EnvelopeRequest {
        const API_KEY: ApiKey = ApiKey::new(58);
    }

    impl RequestResponsePair for EnvelopeRequest {
        type Response = super::EnvelopeResponse;
    }

    impl KafkaDecode for EnvelopeRequest {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            crate::message::ensure_decode_version::<Self>(version)?;

            let request_data = decoder.read_compact_bytes()?;
            let request_principal = decoder.read_compact_nullable_bytes()?;
            let client_host_address = decoder.read_compact_bytes()?;
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                request_data,
                request_principal,
                client_host_address,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for EnvelopeRequest {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            crate::message::ensure_encode_version::<Self>(version)?;

            encoder.write_compact_bytes(&self.request_data)?;
            encoder.write_compact_nullable_bytes(self.request_principal.as_deref())?;
            encoder.write_compact_bytes(&self.client_host_address)?;

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: Self::NAME,
                    version,
                });
            }

            Ok(())
        }
    }
}

/// `EnvelopeResponse` and every struct it declares, under upstream's own names.
///
/// [`EnvelopeResponse`](crate::EnvelopeResponse) is re-exported flat, so this path never has to be
/// written to name the message itself.
pub mod envelope_response {
    use kafka_wire_core::{
        ApiKey, ApiVersion, Bytes, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder,
        KafkaDecode, KafkaEncode, TaggedFields, VersionRange,
    };

    use crate::{KafkaMessage, KafkaResponse};

    /// Response body for the `Envelope` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct EnvelopeResponse {
        /// The embedded response header and data.
        pub response_data: Option<Bytes>,
        /// The error code, or 0 if there was no error.
        pub error_code: i16,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl KafkaMessage for EnvelopeResponse {
        const NAME: &'static str = "EnvelopeResponse";
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 0);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));
    }

    impl KafkaResponse for EnvelopeResponse {
        const API_KEY: ApiKey = ApiKey::new(58);
    }

    impl KafkaDecode for EnvelopeResponse {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            crate::message::ensure_decode_version::<Self>(version)?;

            let response_data = decoder.read_compact_nullable_bytes()?;
            let error_code = decoder.read_i16()?;
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                response_data,
                error_code,
                unknown_tagged_fields,
            })
        }
    }

    impl KafkaEncode for EnvelopeResponse {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            crate::message::ensure_encode_version::<Self>(version)?;

            encoder.write_compact_nullable_bytes(self.response_data.as_deref())?;
            encoder.write_i16(self.error_code)?;

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            } else if !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: Self::NAME,
                    version,
                });
            }

            Ok(())
        }
    }
}

use kafka_wire_core::VersionRange;

use crate::{MessageDescriptor, MessageDirection};

pub use envelope_request::EnvelopeRequest;
pub use envelope_response::EnvelopeResponse;

/// Static metadata for [`EnvelopeRequest`].
pub const ENVELOPE_REQUEST_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    58,
    "EnvelopeRequest",
    MessageDirection::Request,
    VersionRange::new(0, 0),
    Some(VersionRange::new(0, 0)),
);

/// Static metadata for [`EnvelopeResponse`].
pub const ENVELOPE_RESPONSE_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    58,
    "EnvelopeResponse",
    MessageDirection::Response,
    VersionRange::new(0, 0),
    Some(VersionRange::new(0, 0)),
);
