//! @generated by kafka-wire-codegen 0.1.0; do not edit.
//!
//! API key 51 from `AlterUserScramCredentialsRequest.json` and `AlterUserScramCredentialsResponse.json`
//! at Apache Kafka commit `678c0e07e4733c5a592e52046dc2c4e1625587f1`.
//! Request SHA-256: `fcf7ca27e8028316c0957b346c93108c235388d06f829f7ecc5a304407719d26`.
//! Response SHA-256: `f9bba249467a5f499ae10523be22f96aab4ddc6bdff0f54f7cf0be030820ff2d`.

/// `AlterUserScramCredentialsRequest` and every struct it declares, under upstream's own names.
///
/// [`AlterUserScramCredentialsRequest`](crate::AlterUserScramCredentialsRequest) is re-exported flat, so this path never has to be
/// written to name the message itself.
pub mod alter_user_scram_credentials_request {
    use kafka_wire_core::{
        ApiKey, ApiVersion, Bytes, BytesMut, DecodeError, Decoder, EncodeError, EncodeTarget,
        Encoder, KafkaDecode, KafkaEncode, StrBytes, TaggedFields, VersionRange, encode_into_with,
        encoded_len_with,
    };

    use crate::{ApiDescriptor, KafkaMessage, KafkaRequest, RequestResponsePair};

    /// `ScramCredentialDeletion` as declared by the `AlterUserScramCredentials` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct ScramCredentialDeletion {
        /// The user name.
        pub name: StrBytes,
        /// The SCRAM mechanism.
        pub mechanism: i8,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl ScramCredentialDeletion {
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 0);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl ScramCredentialDeletion {
        fn validate_for_version(&self, version: ApiVersion) -> Result<(), EncodeError> {
            if !Self::SUPPORTED_VERSIONS.contains(version) {
                return Err(EncodeError::UnsupportedVersion {
                    message: "ScramCredentialDeletion",
                    version,
                    supported: Self::SUPPORTED_VERSIONS,
                });
            }

            if !Self::is_flexible(version) && !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "ScramCredentialDeletion",
                    version,
                });
            }

            Ok(())
        }
    }

    impl KafkaDecode for ScramCredentialDeletion {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            if !Self::SUPPORTED_VERSIONS.contains(version) {
                return Err(DecodeError::UnsupportedVersion {
                    message: "ScramCredentialDeletion",
                    version,
                    supported: Self::SUPPORTED_VERSIONS,
                });
            }

            let name = decoder.read_compact_string()?;
            let mechanism = decoder.read_i8()?;
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                name,
                mechanism,
                unknown_tagged_fields,
            })
        }
    }

    impl ScramCredentialDeletion {
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_compact_string(&self.name)?;
            encoder.write_i8(self.mechanism)?;

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            }

            Ok(())
        }
    }

    impl KafkaEncode for ScramCredentialDeletion {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            self.validate_for_version(version)?;
            ScramCredentialDeletion::encode_validated(self, encoder, version)
        }

        fn encoded_len(&self, version: ApiVersion) -> Result<usize, EncodeError> {
            encoded_len_with(
                || self.validate_for_version(version),
                |encoder| ScramCredentialDeletion::encode_validated(self, encoder, version),
            )
        }

        fn encode_into(
            &self,
            buffer: &mut BytesMut,
            version: ApiVersion,
        ) -> Result<usize, EncodeError> {
            encode_into_with(
                buffer,
                || self.validate_for_version(version),
                |encoder| ScramCredentialDeletion::encode_validated(self, encoder, version),
                |encoder| ScramCredentialDeletion::encode_validated(self, encoder, version),
            )
        }
    }

    /// `ScramCredentialUpsertion` as declared by the `AlterUserScramCredentials` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct ScramCredentialUpsertion {
        /// The user name.
        pub name: StrBytes,
        /// The SCRAM mechanism.
        pub mechanism: i8,
        /// The number of iterations.
        pub iterations: i32,
        /// A random salt generated by the client.
        pub salt: Bytes,
        /// The salted password.
        pub salted_password: Bytes,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl ScramCredentialUpsertion {
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 0);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl ScramCredentialUpsertion {
        fn validate_for_version(&self, version: ApiVersion) -> Result<(), EncodeError> {
            if !Self::SUPPORTED_VERSIONS.contains(version) {
                return Err(EncodeError::UnsupportedVersion {
                    message: "ScramCredentialUpsertion",
                    version,
                    supported: Self::SUPPORTED_VERSIONS,
                });
            }

            if !Self::is_flexible(version) && !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "ScramCredentialUpsertion",
                    version,
                });
            }

            Ok(())
        }
    }

    impl KafkaDecode for ScramCredentialUpsertion {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            if !Self::SUPPORTED_VERSIONS.contains(version) {
                return Err(DecodeError::UnsupportedVersion {
                    message: "ScramCredentialUpsertion",
                    version,
                    supported: Self::SUPPORTED_VERSIONS,
                });
            }

            let name = decoder.read_compact_string()?;
            let mechanism = decoder.read_i8()?;
            let iterations = decoder.read_i32()?;
            let salt = decoder.read_compact_bytes()?;
            let salted_password = decoder.read_compact_bytes()?;
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                name,
                mechanism,
                iterations,
                salt,
                salted_password,
                unknown_tagged_fields,
            })
        }
    }

    impl ScramCredentialUpsertion {
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_compact_string(&self.name)?;
            encoder.write_i8(self.mechanism)?;
            encoder.write_i32(self.iterations)?;
            encoder.write_compact_bytes(&self.salt)?;
            encoder.write_compact_bytes(&self.salted_password)?;

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            }

            Ok(())
        }
    }

    impl KafkaEncode for ScramCredentialUpsertion {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            self.validate_for_version(version)?;
            ScramCredentialUpsertion::encode_validated(self, encoder, version)
        }

        fn encoded_len(&self, version: ApiVersion) -> Result<usize, EncodeError> {
            encoded_len_with(
                || self.validate_for_version(version),
                |encoder| ScramCredentialUpsertion::encode_validated(self, encoder, version),
            )
        }

        fn encode_into(
            &self,
            buffer: &mut BytesMut,
            version: ApiVersion,
        ) -> Result<usize, EncodeError> {
            encode_into_with(
                buffer,
                || self.validate_for_version(version),
                |encoder| ScramCredentialUpsertion::encode_validated(self, encoder, version),
                |encoder| ScramCredentialUpsertion::encode_validated(self, encoder, version),
            )
        }
    }

    /// Request body for the `AlterUserScramCredentials` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct AlterUserScramCredentialsRequest {
        /// The SCRAM credentials to remove.
        pub deletions: Vec<ScramCredentialDeletion>,
        /// The SCRAM credentials to update/insert.
        pub upsertions: Vec<ScramCredentialUpsertion>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl KafkaMessage for AlterUserScramCredentialsRequest {
        const NAME: &'static str = "AlterUserScramCredentialsRequest";
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 0);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));
    }

    impl KafkaRequest for AlterUserScramCredentialsRequest {
        const API_KEY: ApiKey = ApiKey::new(51);
        const API_DESCRIPTOR: &'static ApiDescriptor =
            &super::ALTER_USER_SCRAM_CREDENTIALS_API_DESCRIPTOR;
    }

    impl RequestResponsePair for AlterUserScramCredentialsRequest {
        type Response = super::AlterUserScramCredentialsResponse;
    }

    impl AlterUserScramCredentialsRequest {
        fn validate_for_version(&self, version: ApiVersion) -> Result<(), EncodeError> {
            crate::message::ensure_encode_version::<Self>(version)?;

            for value in &self.deletions {
                value.validate_for_version(version)?;
            }
            for value in &self.upsertions {
                value.validate_for_version(version)?;
            }
            if !Self::is_flexible(version) && !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: Self::NAME,
                    version,
                });
            }

            Ok(())
        }
    }

    impl KafkaDecode for AlterUserScramCredentialsRequest {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            crate::message::ensure_decode_version::<Self>(version)?;

            let deletions = {
                let length = decoder.read_compact_array_len()?;
                decoder.read_vec(length, |decoder| {
                    ScramCredentialDeletion::decode(decoder, version)
                })?
            };
            let upsertions = {
                let length = decoder.read_compact_array_len()?;
                decoder.read_vec(length, |decoder| {
                    ScramCredentialUpsertion::decode(decoder, version)
                })?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                deletions,
                upsertions,
                unknown_tagged_fields,
            })
        }
    }

    impl AlterUserScramCredentialsRequest {
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_compact_array_len(self.deletions.len())?;
            for value in &self.deletions {
                value.encode_validated(encoder, version)?;
            }
            encoder.write_compact_array_len(self.upsertions.len())?;
            for value in &self.upsertions {
                value.encode_validated(encoder, version)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            }

            Ok(())
        }
    }

    impl KafkaEncode for AlterUserScramCredentialsRequest {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            self.validate_for_version(version)?;
            AlterUserScramCredentialsRequest::encode_validated(self, encoder, version)
        }

        fn encoded_len(&self, version: ApiVersion) -> Result<usize, EncodeError> {
            encoded_len_with(
                || self.validate_for_version(version),
                |encoder| {
                    AlterUserScramCredentialsRequest::encode_validated(self, encoder, version)
                },
            )
        }

        fn encode_into(
            &self,
            buffer: &mut BytesMut,
            version: ApiVersion,
        ) -> Result<usize, EncodeError> {
            encode_into_with(
                buffer,
                || self.validate_for_version(version),
                |encoder| {
                    AlterUserScramCredentialsRequest::encode_validated(self, encoder, version)
                },
                |encoder| {
                    AlterUserScramCredentialsRequest::encode_validated(self, encoder, version)
                },
            )
        }
    }
}

/// `AlterUserScramCredentialsResponse` and every struct it declares, under upstream's own names.
///
/// [`AlterUserScramCredentialsResponse`](crate::AlterUserScramCredentialsResponse) is re-exported flat, so this path never has to be
/// written to name the message itself.
pub mod alter_user_scram_credentials_response {
    use kafka_wire_core::{
        ApiKey, ApiVersion, BytesMut, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder,
        KafkaDecode, KafkaEncode, StrBytes, TaggedFields, VersionRange, encode_into_with,
        encoded_len_with,
    };

    use crate::{KafkaMessage, KafkaResponse};

    /// `AlterUserScramCredentialsResult` as declared by the `AlterUserScramCredentials` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub struct AlterUserScramCredentialsResult {
        /// The user name.
        pub user: StrBytes,
        /// The error code.
        pub error_code: i16,
        /// The error message, if any.
        pub error_message: Option<StrBytes>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl AlterUserScramCredentialsResult {
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 0);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));

        fn is_flexible(version: ApiVersion) -> bool {
            Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
        }
    }

    impl AlterUserScramCredentialsResult {
        fn validate_for_version(&self, version: ApiVersion) -> Result<(), EncodeError> {
            if !Self::SUPPORTED_VERSIONS.contains(version) {
                return Err(EncodeError::UnsupportedVersion {
                    message: "AlterUserScramCredentialsResult",
                    version,
                    supported: Self::SUPPORTED_VERSIONS,
                });
            }

            if !Self::is_flexible(version) && !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: "AlterUserScramCredentialsResult",
                    version,
                });
            }

            Ok(())
        }
    }

    impl Default for AlterUserScramCredentialsResult {
        fn default() -> Self {
            Self {
                user: StrBytes::default(),
                error_code: 0,
                error_message: Some(StrBytes::default()),
                unknown_tagged_fields: TaggedFields::default(),
            }
        }
    }

    impl KafkaDecode for AlterUserScramCredentialsResult {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            if !Self::SUPPORTED_VERSIONS.contains(version) {
                return Err(DecodeError::UnsupportedVersion {
                    message: "AlterUserScramCredentialsResult",
                    version,
                    supported: Self::SUPPORTED_VERSIONS,
                });
            }

            let user = decoder.read_compact_string()?;
            let error_code = decoder.read_i16()?;
            let error_message = decoder.read_compact_nullable_string()?;
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                user,
                error_code,
                error_message,
                unknown_tagged_fields,
            })
        }
    }

    impl AlterUserScramCredentialsResult {
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_compact_string(&self.user)?;
            encoder.write_i16(self.error_code)?;
            encoder.write_compact_nullable_string(self.error_message.as_ref())?;

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            }

            Ok(())
        }
    }

    impl KafkaEncode for AlterUserScramCredentialsResult {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            self.validate_for_version(version)?;
            AlterUserScramCredentialsResult::encode_validated(self, encoder, version)
        }

        fn encoded_len(&self, version: ApiVersion) -> Result<usize, EncodeError> {
            encoded_len_with(
                || self.validate_for_version(version),
                |encoder| AlterUserScramCredentialsResult::encode_validated(self, encoder, version),
            )
        }

        fn encode_into(
            &self,
            buffer: &mut BytesMut,
            version: ApiVersion,
        ) -> Result<usize, EncodeError> {
            encode_into_with(
                buffer,
                || self.validate_for_version(version),
                |encoder| AlterUserScramCredentialsResult::encode_validated(self, encoder, version),
                |encoder| AlterUserScramCredentialsResult::encode_validated(self, encoder, version),
            )
        }
    }

    /// Response body for the `AlterUserScramCredentials` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct AlterUserScramCredentialsResponse {
        /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
        pub throttle_time_ms: i32,
        /// The results for deletions and alterations, one per affected user.
        pub results: Vec<AlterUserScramCredentialsResult>,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl KafkaMessage for AlterUserScramCredentialsResponse {
        const NAME: &'static str = "AlterUserScramCredentialsResponse";
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 0);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));
    }

    impl KafkaResponse for AlterUserScramCredentialsResponse {
        const API_KEY: ApiKey = ApiKey::new(51);
    }

    impl AlterUserScramCredentialsResponse {
        fn validate_for_version(&self, version: ApiVersion) -> Result<(), EncodeError> {
            crate::message::ensure_encode_version::<Self>(version)?;

            for value in &self.results {
                value.validate_for_version(version)?;
            }
            if !Self::is_flexible(version) && !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: Self::NAME,
                    version,
                });
            }

            Ok(())
        }
    }

    impl KafkaDecode for AlterUserScramCredentialsResponse {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            crate::message::ensure_decode_version::<Self>(version)?;

            let throttle_time_ms = decoder.read_i32()?;
            let results = {
                let length = decoder.read_compact_array_len()?;
                decoder.read_vec(length, |decoder| {
                    AlterUserScramCredentialsResult::decode(decoder, version)
                })?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                throttle_time_ms,
                results,
                unknown_tagged_fields,
            })
        }
    }

    impl AlterUserScramCredentialsResponse {
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_i32(self.throttle_time_ms)?;
            encoder.write_compact_array_len(self.results.len())?;
            for value in &self.results {
                value.encode_validated(encoder, version)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            }

            Ok(())
        }
    }

    impl KafkaEncode for AlterUserScramCredentialsResponse {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            self.validate_for_version(version)?;
            AlterUserScramCredentialsResponse::encode_validated(self, encoder, version)
        }

        fn encoded_len(&self, version: ApiVersion) -> Result<usize, EncodeError> {
            encoded_len_with(
                || self.validate_for_version(version),
                |encoder| {
                    AlterUserScramCredentialsResponse::encode_validated(self, encoder, version)
                },
            )
        }

        fn encode_into(
            &self,
            buffer: &mut BytesMut,
            version: ApiVersion,
        ) -> Result<usize, EncodeError> {
            encode_into_with(
                buffer,
                || self.validate_for_version(version),
                |encoder| {
                    AlterUserScramCredentialsResponse::encode_validated(self, encoder, version)
                },
                |encoder| {
                    AlterUserScramCredentialsResponse::encode_validated(self, encoder, version)
                },
            )
        }
    }
}

use kafka_wire_core::VersionRange;

use crate::{ApiDescriptor, MessageDescriptor, MessageDirection};

pub use alter_user_scram_credentials_request::AlterUserScramCredentialsRequest;
pub use alter_user_scram_credentials_response::AlterUserScramCredentialsResponse;

/// Static metadata for [`AlterUserScramCredentialsRequest`].
pub const ALTER_USER_SCRAM_CREDENTIALS_REQUEST_DESCRIPTOR: MessageDescriptor =
    MessageDescriptor::new(
        51,
        "AlterUserScramCredentialsRequest",
        MessageDirection::Request,
        VersionRange::new(0, 0),
        Some(VersionRange::new(0, 0)),
    );

/// Static metadata for [`AlterUserScramCredentialsResponse`].
pub const ALTER_USER_SCRAM_CREDENTIALS_RESPONSE_DESCRIPTOR: MessageDescriptor =
    MessageDescriptor::new(
        51,
        "AlterUserScramCredentialsResponse",
        MessageDirection::Response,
        VersionRange::new(0, 0),
        Some(VersionRange::new(0, 0)),
    );

/// Static pair metadata for the `AlterUserScramCredentials` API.
pub const ALTER_USER_SCRAM_CREDENTIALS_API_DESCRIPTOR: ApiDescriptor = ApiDescriptor::new(
    51,
    &ALTER_USER_SCRAM_CREDENTIALS_REQUEST_DESCRIPTOR,
    &ALTER_USER_SCRAM_CREDENTIALS_RESPONSE_DESCRIPTOR,
    VersionRange::new(0, 0),
    Some(VersionRange::new(0, 0)),
    false,
);
