//! @generated by kafka-wire-codegen 0.1.0; do not edit.
//!
//! API key 75 from `DescribeTopicPartitionsRequest.json` and `DescribeTopicPartitionsResponse.json`
//! at Apache Kafka commit `678c0e07e4733c5a592e52046dc2c4e1625587f1`.
//! Request SHA-256: `315ee63211edd8aa21f9c5695f46a2b21bb964ee8be39b509a9bfe46915343b8`.
//! Response SHA-256: `c3968b9ccce25dda201aaca3e6be7561eae44d494bad20fc6c9d671a2dceb898`.

use kafka_wire_core::{
    ApiKey, ApiVersion, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder, KafkaDecode,
    KafkaEncode, StrBytes, TaggedFields, Uuid, VersionRange,
};

use crate::{
    KafkaMessage, KafkaRequest, KafkaResponse, MessageDescriptor, MessageDirection,
    RequestResponsePair,
};

/// `DescribeTopicPartitionsRequestTopicRequest` as declared by the `DescribeTopicPartitions` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct DescribeTopicPartitionsRequestTopicRequest {
    /// The topic name.
    pub name: StrBytes,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl DescribeTopicPartitionsRequestTopicRequest {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for DescribeTopicPartitionsRequestTopicRequest {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let name = decoder.read_compact_string()?;
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            name,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for DescribeTopicPartitionsRequestTopicRequest {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        encoder.write_compact_string(&self.name)?;

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        }

        Ok(())
    }
}

/// `DescribeTopicPartitionsRequestCursor` as declared by the `DescribeTopicPartitions` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct DescribeTopicPartitionsRequestCursor {
    /// The name for the first topic to process.
    pub topic_name: StrBytes,
    /// The partition index to start with.
    pub partition_index: i32,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl DescribeTopicPartitionsRequestCursor {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for DescribeTopicPartitionsRequestCursor {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let topic_name = decoder.read_compact_string()?;
        let partition_index = decoder.read_i32()?;
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            topic_name,
            partition_index,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for DescribeTopicPartitionsRequestCursor {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        encoder.write_compact_string(&self.topic_name)?;
        encoder.write_i32(self.partition_index)?;

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        }

        Ok(())
    }
}

/// Request body for the `DescribeTopicPartitions` API.
#[non_exhaustive]
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct DescribeTopicPartitionsRequest {
    /// The topics to fetch details for.
    pub topics: Vec<DescribeTopicPartitionsRequestTopicRequest>,
    /// The maximum number of partitions included in the response.
    pub response_partition_limit: i32,
    /// The first topic and partition index to fetch details for.
    pub cursor: Option<DescribeTopicPartitionsRequestCursor>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl Default for DescribeTopicPartitionsRequest {
    fn default() -> Self {
        Self {
            topics: Vec::new(),
            response_partition_limit: 2_000,
            cursor: None,
            unknown_tagged_fields: TaggedFields::default(),
        }
    }
}

impl KafkaMessage for DescribeTopicPartitionsRequest {
    const NAME: &'static str = "DescribeTopicPartitionsRequest";
    const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 0);
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));
}

impl KafkaRequest for DescribeTopicPartitionsRequest {
    const API_KEY: ApiKey = ApiKey::new(75);
}

impl RequestResponsePair for DescribeTopicPartitionsRequest {
    type Response = DescribeTopicPartitionsResponse;
}

impl KafkaDecode for DescribeTopicPartitionsRequest {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        crate::message::ensure_decode_version::<Self>(version)?;

        let topics = {
            let length = decoder.read_compact_array_len()?;
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(DescribeTopicPartitionsRequestTopicRequest::decode(
                    decoder, version,
                )?);
            }
            values
        };
        let response_partition_limit = decoder.read_i32()?;
        let cursor = if decoder.read_struct_presence()? {
            Some(DescribeTopicPartitionsRequestCursor::decode(
                decoder, version,
            )?)
        } else {
            None
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            topics,
            response_partition_limit,
            cursor,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for DescribeTopicPartitionsRequest {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        crate::message::ensure_encode_version::<Self>(version)?;

        encoder.write_compact_array_len(self.topics.len())?;
        for value in &self.topics {
            value.encode(encoder, version)?;
        }
        encoder.write_i32(self.response_partition_limit)?;
        if let Some(value) = &self.cursor {
            encoder.write_struct_presence(true)?;
            value.encode(encoder, version)?;
        } else {
            encoder.write_struct_presence(false)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: Self::NAME,
                version,
            });
        }

        Ok(())
    }
}

/// `DescribeTopicPartitionsResponseTopic` as declared by the `DescribeTopicPartitions` API.
#[non_exhaustive]
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct DescribeTopicPartitionsResponseTopic {
    /// The topic error, or 0 if there was no error.
    pub error_code: i16,
    /// The topic name.
    pub name: Option<StrBytes>,
    /// The topic id.
    pub topic_id: Uuid,
    /// True if the topic is internal.
    pub is_internal: bool,
    /// Each partition in the topic.
    pub partitions: Vec<DescribeTopicPartitionsResponsePartition>,
    /// 32-bit bitfield to represent authorized operations for this topic.
    pub topic_authorized_operations: i32,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl DescribeTopicPartitionsResponseTopic {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl Default for DescribeTopicPartitionsResponseTopic {
    fn default() -> Self {
        Self {
            error_code: 0,
            name: Some(StrBytes::default()),
            topic_id: Uuid::ZERO,
            is_internal: false,
            partitions: Vec::new(),
            topic_authorized_operations: -2_147_483_648,
            unknown_tagged_fields: TaggedFields::default(),
        }
    }
}

impl KafkaDecode for DescribeTopicPartitionsResponseTopic {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let error_code = decoder.read_i16()?;
        let name = decoder.read_compact_nullable_string()?;
        let topic_id = decoder.read_uuid()?;
        let is_internal = decoder.read_bool()?;
        let partitions = {
            let length = decoder.read_compact_array_len()?;
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(DescribeTopicPartitionsResponsePartition::decode(
                    decoder, version,
                )?);
            }
            values
        };
        let topic_authorized_operations = decoder.read_i32()?;
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            error_code,
            name,
            topic_id,
            is_internal,
            partitions,
            topic_authorized_operations,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for DescribeTopicPartitionsResponseTopic {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        encoder.write_i16(self.error_code)?;
        encoder.write_compact_nullable_string(self.name.as_ref())?;
        encoder.write_uuid(self.topic_id)?;
        encoder.write_bool(self.is_internal)?;
        encoder.write_compact_array_len(self.partitions.len())?;
        for value in &self.partitions {
            value.encode(encoder, version)?;
        }
        encoder.write_i32(self.topic_authorized_operations)?;

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        }

        Ok(())
    }
}

/// `DescribeTopicPartitionsResponsePartition` as declared by the `DescribeTopicPartitions` API.
#[non_exhaustive]
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct DescribeTopicPartitionsResponsePartition {
    /// The partition error, or 0 if there was no error.
    pub error_code: i16,
    /// The partition index.
    pub partition_index: i32,
    /// The ID of the leader broker.
    pub leader_id: i32,
    /// The leader epoch of this partition.
    pub leader_epoch: i32,
    /// The set of all nodes that host this partition.
    pub replica_nodes: Vec<i32>,
    /// The set of nodes that are in sync with the leader for this partition.
    pub isr_nodes: Vec<i32>,
    /// The new eligible leader replicas otherwise.
    pub eligible_leader_replicas: Option<Vec<i32>>,
    /// The last known ELR.
    pub last_known_elr: Option<Vec<i32>>,
    /// The set of offline replicas of this partition.
    pub offline_replicas: Vec<i32>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl DescribeTopicPartitionsResponsePartition {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl Default for DescribeTopicPartitionsResponsePartition {
    fn default() -> Self {
        Self {
            error_code: 0,
            partition_index: 0,
            leader_id: 0,
            leader_epoch: -1,
            replica_nodes: Vec::new(),
            isr_nodes: Vec::new(),
            eligible_leader_replicas: None,
            last_known_elr: None,
            offline_replicas: Vec::new(),
            unknown_tagged_fields: TaggedFields::default(),
        }
    }
}

impl KafkaDecode for DescribeTopicPartitionsResponsePartition {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let error_code = decoder.read_i16()?;
        let partition_index = decoder.read_i32()?;
        let leader_id = decoder.read_i32()?;
        let leader_epoch = decoder.read_i32()?;
        let replica_nodes = {
            let length = decoder.read_compact_array_len()?;
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(decoder.read_i32()?);
            }
            values
        };
        let isr_nodes = {
            let length = decoder.read_compact_array_len()?;
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(decoder.read_i32()?);
            }
            values
        };
        let eligible_leader_replicas = {
            match decoder.read_compact_nullable_array_len()? {
                None => None,
                Some(length) => {
                    let mut values = Vec::with_capacity(length);
                    for _ in 0..length {
                        values.push(decoder.read_i32()?);
                    }
                    Some(values)
                }
            }
        };
        let last_known_elr = {
            match decoder.read_compact_nullable_array_len()? {
                None => None,
                Some(length) => {
                    let mut values = Vec::with_capacity(length);
                    for _ in 0..length {
                        values.push(decoder.read_i32()?);
                    }
                    Some(values)
                }
            }
        };
        let offline_replicas = {
            let length = decoder.read_compact_array_len()?;
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(decoder.read_i32()?);
            }
            values
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            error_code,
            partition_index,
            leader_id,
            leader_epoch,
            replica_nodes,
            isr_nodes,
            eligible_leader_replicas,
            last_known_elr,
            offline_replicas,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for DescribeTopicPartitionsResponsePartition {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        encoder.write_i16(self.error_code)?;
        encoder.write_i32(self.partition_index)?;
        encoder.write_i32(self.leader_id)?;
        encoder.write_i32(self.leader_epoch)?;
        encoder.write_compact_array_len(self.replica_nodes.len())?;
        for value in &self.replica_nodes {
            encoder.write_i32(*value)?;
        }
        encoder.write_compact_array_len(self.isr_nodes.len())?;
        for value in &self.isr_nodes {
            encoder.write_i32(*value)?;
        }
        encoder.write_compact_nullable_array_len(
            self.eligible_leader_replicas.as_ref().map(Vec::len),
        )?;
        if let Some(values) = &self.eligible_leader_replicas {
            for value in values {
                encoder.write_i32(*value)?;
            }
        }
        encoder.write_compact_nullable_array_len(self.last_known_elr.as_ref().map(Vec::len))?;
        if let Some(values) = &self.last_known_elr {
            for value in values {
                encoder.write_i32(*value)?;
            }
        }
        encoder.write_compact_array_len(self.offline_replicas.len())?;
        for value in &self.offline_replicas {
            encoder.write_i32(*value)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        }

        Ok(())
    }
}

/// `DescribeTopicPartitionsResponseCursor` as declared by the `DescribeTopicPartitions` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct DescribeTopicPartitionsResponseCursor {
    /// The name for the first topic to process.
    pub topic_name: StrBytes,
    /// The partition index to start with.
    pub partition_index: i32,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl DescribeTopicPartitionsResponseCursor {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for DescribeTopicPartitionsResponseCursor {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let topic_name = decoder.read_compact_string()?;
        let partition_index = decoder.read_i32()?;
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            topic_name,
            partition_index,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for DescribeTopicPartitionsResponseCursor {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        encoder.write_compact_string(&self.topic_name)?;
        encoder.write_i32(self.partition_index)?;

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        }

        Ok(())
    }
}

/// Response body for the `DescribeTopicPartitions` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct DescribeTopicPartitionsResponse {
    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    pub throttle_time_ms: i32,
    /// Each topic in the response.
    pub topics: Vec<DescribeTopicPartitionsResponseTopic>,
    /// The next topic and partition index to fetch details for.
    pub next_cursor: Option<DescribeTopicPartitionsResponseCursor>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl KafkaMessage for DescribeTopicPartitionsResponse {
    const NAME: &'static str = "DescribeTopicPartitionsResponse";
    const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 0);
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(0, 0));
}

impl KafkaResponse for DescribeTopicPartitionsResponse {
    const API_KEY: ApiKey = ApiKey::new(75);
}

impl KafkaDecode for DescribeTopicPartitionsResponse {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        crate::message::ensure_decode_version::<Self>(version)?;

        let throttle_time_ms = decoder.read_i32()?;
        let topics = {
            let length = decoder.read_compact_array_len()?;
            let mut values = Vec::with_capacity(length);
            for _ in 0..length {
                values.push(DescribeTopicPartitionsResponseTopic::decode(
                    decoder, version,
                )?);
            }
            values
        };
        let next_cursor = if decoder.read_struct_presence()? {
            Some(DescribeTopicPartitionsResponseCursor::decode(
                decoder, version,
            )?)
        } else {
            None
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            throttle_time_ms,
            topics,
            next_cursor,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for DescribeTopicPartitionsResponse {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        crate::message::ensure_encode_version::<Self>(version)?;

        encoder.write_i32(self.throttle_time_ms)?;
        encoder.write_compact_array_len(self.topics.len())?;
        for value in &self.topics {
            value.encode(encoder, version)?;
        }
        if let Some(value) = &self.next_cursor {
            encoder.write_struct_presence(true)?;
            value.encode(encoder, version)?;
        } else {
            encoder.write_struct_presence(false)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: Self::NAME,
                version,
            });
        }

        Ok(())
    }
}

/// Static metadata for [`DescribeTopicPartitionsRequest`].
pub const DESCRIBE_TOPIC_PARTITIONS_REQUEST_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    75,
    "DescribeTopicPartitionsRequest",
    MessageDirection::Request,
    VersionRange::new(0, 0),
    Some(VersionRange::new(0, 0)),
);

/// Static metadata for [`DescribeTopicPartitionsResponse`].
pub const DESCRIBE_TOPIC_PARTITIONS_RESPONSE_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    75,
    "DescribeTopicPartitionsResponse",
    MessageDirection::Response,
    VersionRange::new(0, 0),
    Some(VersionRange::new(0, 0)),
);
