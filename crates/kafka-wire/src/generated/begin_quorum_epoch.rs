//! @generated by kafka-wire-codegen 0.1.0; do not edit.
//!
//! API key 53 from `BeginQuorumEpochRequest.json` and `BeginQuorumEpochResponse.json`
//! at Apache Kafka commit `678c0e07e4733c5a592e52046dc2c4e1625587f1`.
//! Request SHA-256: `e5b5fcc284b5002bed458480a4a80580aef2e7739fc95416e96c19dd118053ea`.
//! Response SHA-256: `89a187c3df2c35ecd2d2d6659c4666f565045e946269fa1bd905b2b511f83a99`.

use kafka_wire_core::{
    ApiKey, ApiVersion, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder, KafkaDecode,
    KafkaEncode, KnownTags, StrBytes, TagOutcome, TaggedFields, Uuid, VersionRange,
};

use crate::{
    KafkaMessage, KafkaRequest, KafkaResponse, MessageDescriptor, MessageDirection,
    RequestResponsePair,
};

/// `TopicData` as declared by the `BeginQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct BeginQuorumEpochRequestTopicData {
    /// The topic name.
    pub topic_name: StrBytes,
    /// The partitions.
    pub partitions: Vec<BeginQuorumEpochRequestPartitionData>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl BeginQuorumEpochRequestTopicData {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for BeginQuorumEpochRequestTopicData {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let topic_name = if Self::is_flexible(version) {
            decoder.read_compact_string()?
        } else {
            decoder.read_string()?
        };
        let partitions = {
            let length = if Self::is_flexible(version) {
                decoder.read_compact_array_len()?
            } else {
                decoder.read_array_len()?
            };
            decoder.read_vec(length, |decoder| {
                BeginQuorumEpochRequestPartitionData::decode(decoder, version)
            })?
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            topic_name,
            partitions,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for BeginQuorumEpochRequestTopicData {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        if Self::is_flexible(version) {
            encoder.write_compact_string(&self.topic_name)?;
        } else {
            encoder.write_string(&self.topic_name)?;
        }
        if Self::is_flexible(version) {
            encoder.write_compact_array_len(self.partitions.len())?;
        } else {
            encoder.write_array_len(self.partitions.len())?;
        }
        for value in &self.partitions {
            value.encode(encoder, version)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "BeginQuorumEpochRequestTopicData",
                version,
            });
        }

        Ok(())
    }
}

/// `PartitionData` as declared by the `BeginQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct BeginQuorumEpochRequestPartitionData {
    /// The partition index.
    pub partition_index: i32,
    /// The directory id of the receiving replica.
    pub voter_directory_id: Uuid,
    /// The ID of the newly elected leader.
    pub leader_id: i32,
    /// The epoch of the newly elected leader.
    pub leader_epoch: i32,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl BeginQuorumEpochRequestPartitionData {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for BeginQuorumEpochRequestPartitionData {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let partition_index = decoder.read_i32()?;
        let voter_directory_id = if version.value() >= 1 {
            decoder.read_uuid()?
        } else {
            Uuid::ZERO
        };
        let leader_id = decoder.read_i32()?;
        let leader_epoch = decoder.read_i32()?;
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            partition_index,
            voter_directory_id,
            leader_id,
            leader_epoch,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for BeginQuorumEpochRequestPartitionData {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        encoder.write_i32(self.partition_index)?;
        if version.value() >= 1 {
            encoder.write_uuid(self.voter_directory_id)?;
        }
        encoder.write_i32(self.leader_id)?;
        encoder.write_i32(self.leader_epoch)?;

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "BeginQuorumEpochRequestPartitionData",
                version,
            });
        }

        Ok(())
    }
}

/// `LeaderEndpoint` as declared by the `BeginQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct BeginQuorumEpochRequestLeaderEndpoint {
    /// The name of the endpoint.
    pub name: StrBytes,
    /// The node's hostname.
    pub host: StrBytes,
    /// The node's port.
    pub port: u16,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl BeginQuorumEpochRequestLeaderEndpoint {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for BeginQuorumEpochRequestLeaderEndpoint {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let name = if version.value() >= 1 {
            decoder.read_compact_string()?
        } else {
            StrBytes::default()
        };
        let host = if version.value() >= 1 {
            decoder.read_compact_string()?
        } else {
            StrBytes::default()
        };
        let port = if version.value() >= 1 {
            decoder.read_u16()?
        } else {
            0
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            name,
            host,
            port,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for BeginQuorumEpochRequestLeaderEndpoint {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        if version.value() >= 1 {
            encoder.write_compact_string(&self.name)?;
        }
        if version.value() >= 1 {
            encoder.write_compact_string(&self.host)?;
        }
        if version.value() >= 1 {
            encoder.write_u16(self.port)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "BeginQuorumEpochRequestLeaderEndpoint",
                version,
            });
        }

        Ok(())
    }
}

/// Request body for the `BeginQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct BeginQuorumEpochRequest {
    /// The cluster id.
    pub cluster_id: Option<StrBytes>,
    /// The replica id of the voter receiving the request.
    pub voter_id: i32,
    /// The topics.
    pub topics: Vec<BeginQuorumEpochRequestTopicData>,
    /// Endpoints for the leader.
    pub leader_endpoints: Vec<BeginQuorumEpochRequestLeaderEndpoint>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl Default for BeginQuorumEpochRequest {
    fn default() -> Self {
        Self {
            cluster_id: None,
            voter_id: -1,
            topics: Vec::new(),
            leader_endpoints: Vec::new(),
            unknown_tagged_fields: TaggedFields::default(),
        }
    }
}

impl KafkaMessage for BeginQuorumEpochRequest {
    const NAME: &'static str = "BeginQuorumEpochRequest";
    const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 1);
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));
}

impl KafkaRequest for BeginQuorumEpochRequest {
    const API_KEY: ApiKey = ApiKey::new(53);
}

impl RequestResponsePair for BeginQuorumEpochRequest {
    type Response = BeginQuorumEpochResponse;
}

impl KafkaDecode for BeginQuorumEpochRequest {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        crate::message::ensure_decode_version::<Self>(version)?;

        let cluster_id = if Self::is_flexible(version) {
            decoder.read_compact_nullable_string()?
        } else {
            decoder.read_nullable_string()?
        };
        let voter_id = if version.value() >= 1 {
            decoder.read_i32()?
        } else {
            -1
        };
        let topics = {
            let length = if Self::is_flexible(version) {
                decoder.read_compact_array_len()?
            } else {
                decoder.read_array_len()?
            };
            decoder.read_vec(length, |decoder| {
                BeginQuorumEpochRequestTopicData::decode(decoder, version)
            })?
        };
        let leader_endpoints = if version.value() >= 1 {
            let length = decoder.read_compact_array_len()?;
            decoder.read_vec(length, |decoder| {
                BeginQuorumEpochRequestLeaderEndpoint::decode(decoder, version)
            })?
        } else {
            Vec::new()
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            cluster_id,
            voter_id,
            topics,
            leader_endpoints,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for BeginQuorumEpochRequest {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        crate::message::ensure_encode_version::<Self>(version)?;

        if Self::is_flexible(version) {
            encoder.write_compact_nullable_string(self.cluster_id.as_ref())?;
        } else {
            encoder.write_nullable_string(self.cluster_id.as_ref())?;
        }
        if version.value() >= 1 {
            encoder.write_i32(self.voter_id)?;
        }
        if Self::is_flexible(version) {
            encoder.write_compact_array_len(self.topics.len())?;
        } else {
            encoder.write_array_len(self.topics.len())?;
        }
        for value in &self.topics {
            value.encode(encoder, version)?;
        }
        if version.value() >= 1 {
            encoder.write_compact_array_len(self.leader_endpoints.len())?;
            for value in &self.leader_endpoints {
                value.encode(encoder, version)?;
            }
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: Self::NAME,
                version,
            });
        }

        Ok(())
    }
}

/// `TopicData` as declared by the `BeginQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct BeginQuorumEpochResponseTopicData {
    /// The topic name.
    pub topic_name: StrBytes,
    /// The partition data.
    pub partitions: Vec<BeginQuorumEpochResponsePartitionData>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl BeginQuorumEpochResponseTopicData {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for BeginQuorumEpochResponseTopicData {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let topic_name = if Self::is_flexible(version) {
            decoder.read_compact_string()?
        } else {
            decoder.read_string()?
        };
        let partitions = {
            let length = if Self::is_flexible(version) {
                decoder.read_compact_array_len()?
            } else {
                decoder.read_array_len()?
            };
            decoder.read_vec(length, |decoder| {
                BeginQuorumEpochResponsePartitionData::decode(decoder, version)
            })?
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            topic_name,
            partitions,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for BeginQuorumEpochResponseTopicData {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        if Self::is_flexible(version) {
            encoder.write_compact_string(&self.topic_name)?;
        } else {
            encoder.write_string(&self.topic_name)?;
        }
        if Self::is_flexible(version) {
            encoder.write_compact_array_len(self.partitions.len())?;
        } else {
            encoder.write_array_len(self.partitions.len())?;
        }
        for value in &self.partitions {
            value.encode(encoder, version)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "BeginQuorumEpochResponseTopicData",
                version,
            });
        }

        Ok(())
    }
}

/// `PartitionData` as declared by the `BeginQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct BeginQuorumEpochResponsePartitionData {
    /// The partition index.
    pub partition_index: i32,
    /// The error code for this partition.
    pub error_code: i16,
    /// The ID of the current leader or -1 if the leader is unknown.
    pub leader_id: i32,
    /// The latest known leader epoch.
    pub leader_epoch: i32,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl BeginQuorumEpochResponsePartitionData {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for BeginQuorumEpochResponsePartitionData {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let partition_index = decoder.read_i32()?;
        let error_code = decoder.read_i16()?;
        let leader_id = decoder.read_i32()?;
        let leader_epoch = decoder.read_i32()?;
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            partition_index,
            error_code,
            leader_id,
            leader_epoch,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for BeginQuorumEpochResponsePartitionData {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        encoder.write_i32(self.partition_index)?;
        encoder.write_i16(self.error_code)?;
        encoder.write_i32(self.leader_id)?;
        encoder.write_i32(self.leader_epoch)?;

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "BeginQuorumEpochResponsePartitionData",
                version,
            });
        }

        Ok(())
    }
}

/// `NodeEndpoint` as declared by the `BeginQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct BeginQuorumEpochResponseNodeEndpoint {
    /// The ID of the associated node.
    pub node_id: i32,
    /// The node's hostname.
    pub host: StrBytes,
    /// The node's port.
    pub port: u16,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl BeginQuorumEpochResponseNodeEndpoint {
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));

    fn is_flexible(version: ApiVersion) -> bool {
        Self::FLEXIBLE_VERSIONS.is_some_and(|range| range.contains(version))
    }
}

impl KafkaDecode for BeginQuorumEpochResponseNodeEndpoint {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        let node_id = if version.value() >= 1 {
            decoder.read_i32()?
        } else {
            0
        };
        let host = if version.value() >= 1 {
            decoder.read_compact_string()?
        } else {
            StrBytes::default()
        };
        let port = if version.value() >= 1 {
            decoder.read_u16()?
        } else {
            0
        };
        let unknown_tagged_fields = if Self::is_flexible(version) {
            decoder.read_tagged_fields()?
        } else {
            TaggedFields::default()
        };

        Ok(Self {
            node_id,
            host,
            port,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for BeginQuorumEpochResponseNodeEndpoint {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        if version.value() >= 1 {
            encoder.write_i32(self.node_id)?;
        }
        if version.value() >= 1 {
            encoder.write_compact_string(&self.host)?;
        }
        if version.value() >= 1 {
            encoder.write_u16(self.port)?;
        }

        if Self::is_flexible(version) {
            encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: "BeginQuorumEpochResponseNodeEndpoint",
                version,
            });
        }

        Ok(())
    }
}

/// Response body for the `BeginQuorumEpoch` API.
#[non_exhaustive]
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct BeginQuorumEpochResponse {
    /// The top level error code.
    pub error_code: i16,
    /// The topic data.
    pub topics: Vec<BeginQuorumEpochResponseTopicData>,
    /// Endpoints for all leaders enumerated in `PartitionData`.
    pub node_endpoints: Vec<BeginQuorumEpochResponseNodeEndpoint>,
    /// Unknown flexible-version tagged fields retained for forwarding.
    pub unknown_tagged_fields: TaggedFields,
}

impl KafkaMessage for BeginQuorumEpochResponse {
    const NAME: &'static str = "BeginQuorumEpochResponse";
    const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 1);
    const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(1, 1));
}

impl KafkaResponse for BeginQuorumEpochResponse {
    const API_KEY: ApiKey = ApiKey::new(53);
}

impl KafkaDecode for BeginQuorumEpochResponse {
    fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
        crate::message::ensure_decode_version::<Self>(version)?;

        let error_code = decoder.read_i16()?;
        let topics = {
            let length = if Self::is_flexible(version) {
                decoder.read_compact_array_len()?
            } else {
                decoder.read_array_len()?
            };
            decoder.read_vec(length, |decoder| {
                BeginQuorumEpochResponseTopicData::decode(decoder, version)
            })?
        };
        let mut node_endpoints: Vec<BeginQuorumEpochResponseNodeEndpoint> = Vec::new();
        let mut unknown_tagged_fields = TaggedFields::default();
        if Self::is_flexible(version) {
            unknown_tagged_fields = decoder.read_tagged_fields_with(|tag, decoder| match tag {
                0 => {
                    node_endpoints = {
                        let length = decoder.read_compact_array_len()?;
                        decoder.read_vec(length, |decoder| {
                            BeginQuorumEpochResponseNodeEndpoint::decode(decoder, version)
                        })?
                    };
                    Ok(TagOutcome::Decoded)
                }
                _ => Ok(TagOutcome::Retained),
            })?;
        }

        Ok(Self {
            error_code,
            topics,
            node_endpoints,
            unknown_tagged_fields,
        })
    }
}

impl KafkaEncode for BeginQuorumEpochResponse {
    fn encode<T: EncodeTarget>(
        &self,
        encoder: &mut Encoder<T>,
        version: ApiVersion,
    ) -> Result<(), EncodeError> {
        crate::message::ensure_encode_version::<Self>(version)?;

        if version.value() < 1 && !self.node_endpoints.is_empty() {
            return Err(EncodeError::FieldNotRepresentable {
                message: Self::NAME,
                field: "NodeEndpoints",
                version,
            });
        }

        encoder.write_i16(self.error_code)?;
        if Self::is_flexible(version) {
            encoder.write_compact_array_len(self.topics.len())?;
        } else {
            encoder.write_array_len(self.topics.len())?;
        }
        for value in &self.topics {
            value.encode(encoder, version)?;
        }

        if Self::is_flexible(version) {
            let mut known = KnownTags::new();
            if !self.node_endpoints.is_empty() {
                known.write(0, |encoder| {
                    encoder.write_compact_array_len(self.node_endpoints.len())?;
                    for value in &self.node_endpoints {
                        value.encode(encoder, version)?;
                    }
                    Ok(())
                })?;
            }
            encoder.write_merged_tagged_fields(known, &self.unknown_tagged_fields)?;
        } else if !self.unknown_tagged_fields.is_empty() {
            return Err(EncodeError::TaggedFieldsNotRepresentable {
                message: Self::NAME,
                version,
            });
        }

        Ok(())
    }
}

/// Static metadata for [`BeginQuorumEpochRequest`].
pub const BEGIN_QUORUM_EPOCH_REQUEST_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    53,
    "BeginQuorumEpochRequest",
    MessageDirection::Request,
    VersionRange::new(0, 1),
    Some(VersionRange::new(1, 1)),
);

/// Static metadata for [`BeginQuorumEpochResponse`].
pub const BEGIN_QUORUM_EPOCH_RESPONSE_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    53,
    "BeginQuorumEpochResponse",
    MessageDirection::Response,
    VersionRange::new(0, 1),
    Some(VersionRange::new(1, 1)),
);
