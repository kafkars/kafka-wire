//! @generated by kafka-wire-codegen 0.1.0; do not edit.
//!
//! API key 25 from `AddOffsetsToTxnRequest.json` and `AddOffsetsToTxnResponse.json`
//! at Apache Kafka commit `678c0e07e4733c5a592e52046dc2c4e1625587f1`.
//! Request SHA-256: `80976e06e30975afe6b36ae47cfd4179ff09a0e65416f05870cd6af78ddeadbb`.
//! Response SHA-256: `91ed51b7b350656b7f88ae7e53acbd8bf6a63e3f28d900a18c0dccd82430b063`.

/// `AddOffsetsToTxnRequest` and every struct it declares, under upstream's own names.
///
/// [`AddOffsetsToTxnRequest`](crate::AddOffsetsToTxnRequest) is re-exported flat, so this path never has to be
/// written to name the message itself.
pub mod add_offsets_to_txn_request {
    use kafka_wire_core::{
        ApiKey, ApiVersion, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder, KafkaDecode,
        KafkaEncode, StrBytes, TaggedFields, VersionRange,
    };

    use crate::{KafkaMessage, KafkaRequest, RequestResponsePair};

    /// Request body for the `AddOffsetsToTxn` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct AddOffsetsToTxnRequest {
        /// The transactional id corresponding to the transaction.
        pub transactional_id: StrBytes,
        /// Current producer id in use by the transactional id.
        pub producer_id: i64,
        /// Current epoch associated with the producer id.
        pub producer_epoch: i16,
        /// The unique group identifier.
        pub group_id: StrBytes,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl KafkaMessage for AddOffsetsToTxnRequest {
        const NAME: &'static str = "AddOffsetsToTxnRequest";
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 4);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(3, 4));
    }

    impl KafkaRequest for AddOffsetsToTxnRequest {
        const API_KEY: ApiKey = ApiKey::new(25);
    }

    impl RequestResponsePair for AddOffsetsToTxnRequest {
        type Response = super::AddOffsetsToTxnResponse;
    }

    impl AddOffsetsToTxnRequest {
        fn validate_for_version(&self, version: ApiVersion) -> Result<(), EncodeError> {
            crate::message::ensure_encode_version::<Self>(version)?;

            if !Self::is_flexible(version) && !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: Self::NAME,
                    version,
                });
            }

            Ok(())
        }
    }

    impl KafkaDecode for AddOffsetsToTxnRequest {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            crate::message::ensure_decode_version::<Self>(version)?;

            let transactional_id = if Self::is_flexible(version) {
                decoder.read_compact_string()?
            } else {
                decoder.read_string()?
            };
            let producer_id = decoder.read_i64()?;
            let producer_epoch = decoder.read_i16()?;
            let group_id = if Self::is_flexible(version) {
                decoder.read_compact_string()?
            } else {
                decoder.read_string()?
            };
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                transactional_id,
                producer_id,
                producer_epoch,
                group_id,
                unknown_tagged_fields,
            })
        }
    }

    impl AddOffsetsToTxnRequest {
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            if Self::is_flexible(version) {
                encoder.write_compact_string(&self.transactional_id)?;
            } else {
                encoder.write_string(&self.transactional_id)?;
            }
            encoder.write_i64(self.producer_id)?;
            encoder.write_i16(self.producer_epoch)?;
            if Self::is_flexible(version) {
                encoder.write_compact_string(&self.group_id)?;
            } else {
                encoder.write_string(&self.group_id)?;
            }

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            }

            Ok(())
        }
    }

    impl KafkaEncode for AddOffsetsToTxnRequest {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            self.validate_for_version(version)?;
            AddOffsetsToTxnRequest::encode_validated(self, encoder, version)
        }

        #[doc(hidden)]
        fn validate_encoding(&self, version: ApiVersion) -> Result<(), EncodeError> {
            self.validate_for_version(version)
        }

        #[doc(hidden)]
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            AddOffsetsToTxnRequest::encode_validated(self, encoder, version)
        }
    }
}

/// `AddOffsetsToTxnResponse` and every struct it declares, under upstream's own names.
///
/// [`AddOffsetsToTxnResponse`](crate::AddOffsetsToTxnResponse) is re-exported flat, so this path never has to be
/// written to name the message itself.
pub mod add_offsets_to_txn_response {
    use kafka_wire_core::{
        ApiKey, ApiVersion, DecodeError, Decoder, EncodeError, EncodeTarget, Encoder, KafkaDecode,
        KafkaEncode, TaggedFields, VersionRange,
    };

    use crate::{KafkaMessage, KafkaResponse};

    /// Response body for the `AddOffsetsToTxn` API.
    #[non_exhaustive]
    #[derive(Clone, Debug, Default, Eq, PartialEq)]
    pub struct AddOffsetsToTxnResponse {
        /// Duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
        pub throttle_time_ms: i32,
        /// The response error code, or 0 if there was no error.
        pub error_code: i16,
        /// Unknown flexible-version tagged fields retained for forwarding.
        pub unknown_tagged_fields: TaggedFields,
    }

    impl KafkaMessage for AddOffsetsToTxnResponse {
        const NAME: &'static str = "AddOffsetsToTxnResponse";
        const SUPPORTED_VERSIONS: VersionRange = VersionRange::new(0, 4);
        const FLEXIBLE_VERSIONS: Option<VersionRange> = Some(VersionRange::new(3, 4));
    }

    impl KafkaResponse for AddOffsetsToTxnResponse {
        const API_KEY: ApiKey = ApiKey::new(25);
    }

    impl AddOffsetsToTxnResponse {
        fn validate_for_version(&self, version: ApiVersion) -> Result<(), EncodeError> {
            crate::message::ensure_encode_version::<Self>(version)?;

            if !Self::is_flexible(version) && !self.unknown_tagged_fields.is_empty() {
                return Err(EncodeError::TaggedFieldsNotRepresentable {
                    message: Self::NAME,
                    version,
                });
            }

            Ok(())
        }
    }

    impl KafkaDecode for AddOffsetsToTxnResponse {
        fn decode(decoder: &mut Decoder, version: ApiVersion) -> Result<Self, DecodeError> {
            crate::message::ensure_decode_version::<Self>(version)?;

            let throttle_time_ms = decoder.read_i32()?;
            let error_code = decoder.read_i16()?;
            let unknown_tagged_fields = if Self::is_flexible(version) {
                decoder.read_tagged_fields()?
            } else {
                TaggedFields::default()
            };

            Ok(Self {
                throttle_time_ms,
                error_code,
                unknown_tagged_fields,
            })
        }
    }

    impl AddOffsetsToTxnResponse {
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            encoder.write_i32(self.throttle_time_ms)?;
            encoder.write_i16(self.error_code)?;

            if Self::is_flexible(version) {
                encoder.write_tagged_fields(&self.unknown_tagged_fields)?;
            }

            Ok(())
        }
    }

    impl KafkaEncode for AddOffsetsToTxnResponse {
        fn encode<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            self.validate_for_version(version)?;
            AddOffsetsToTxnResponse::encode_validated(self, encoder, version)
        }

        #[doc(hidden)]
        fn validate_encoding(&self, version: ApiVersion) -> Result<(), EncodeError> {
            self.validate_for_version(version)
        }

        #[doc(hidden)]
        fn encode_validated<T: EncodeTarget>(
            &self,
            encoder: &mut Encoder<T>,
            version: ApiVersion,
        ) -> Result<(), EncodeError> {
            AddOffsetsToTxnResponse::encode_validated(self, encoder, version)
        }
    }
}

use kafka_wire_core::VersionRange;

use crate::{MessageDescriptor, MessageDirection};

pub use add_offsets_to_txn_request::AddOffsetsToTxnRequest;
pub use add_offsets_to_txn_response::AddOffsetsToTxnResponse;

/// Static metadata for [`AddOffsetsToTxnRequest`].
pub const ADD_OFFSETS_TO_TXN_REQUEST_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    25,
    "AddOffsetsToTxnRequest",
    MessageDirection::Request,
    VersionRange::new(0, 4),
    Some(VersionRange::new(3, 4)),
);

/// Static metadata for [`AddOffsetsToTxnResponse`].
pub const ADD_OFFSETS_TO_TXN_RESPONSE_DESCRIPTOR: MessageDescriptor = MessageDescriptor::new(
    25,
    "AddOffsetsToTxnResponse",
    MessageDirection::Response,
    VersionRange::new(0, 4),
    Some(VersionRange::new(3, 4)),
);
