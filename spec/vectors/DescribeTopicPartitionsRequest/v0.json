{
  "schema": 1,
  "vectors": [
    {
      "name": "synthesized_defaults_v0",
      "why": "Every field this version declares, at its protocol default. Synthesized so the message is held to Apache Kafka's bytes at all; hand-written cases carry the interesting shapes.",
      "message": "DescribeTopicPartitionsRequest",
      "api_key": 75,
      "direction": "request",
      "version": 0,
      "flexible": true,
      "json_value": {
        "cursor": null,
        "responsePartitionLimit": 2000,
        "topics": []
      },
      "hex": "01000007d0ff00"
    },
    {
      "name": "a_present_cursor",
      "why": "The nullable struct is present, so the wire carries the one-byte marker followed by the struct body. This is the half no synthesized case reaches, and it is where a marker spelled as a varint would diverge from Kafka: both spellings are one byte, so only Kafka's own encoder can tell them apart.",
      "message": "DescribeTopicPartitionsRequest",
      "api_key": 75,
      "direction": "request",
      "version": 0,
      "flexible": true,
      "json_value": {
        "cursor": {
          "partitionIndex": 7,
          "topicName": "orders"
        },
        "responsePartitionLimit": 2000,
        "topics": [
          {
            "name": "orders"
          }
        ]
      },
      "hex": "02076f726465727300000007d001076f7264657273000000070000"
    },
    {
      "name": "synthesized_populated_v0",
      "why": "Every field this version declares, present and non-default: one element in every array, every nullable thing supplied. The defaults case leaves arrays empty, which hides every field of the struct the array would have held — a records field encoded with the wrong length prefix survived the whole corpus that way, because nothing ever reached the struct holding it.",
      "message": "DescribeTopicPartitionsRequest",
      "api_key": 75,
      "direction": "request",
      "version": 0,
      "flexible": true,
      "json_value": {
        "cursor": {
          "partitionIndex": 1,
          "topicName": "v"
        },
        "responsePartitionLimit": 1,
        "topics": [
          {
            "name": "v"
          }
        ]
      },
      "hex": "0202760000000001010276000000010000"
    }
  ]
}
