{
  "schema": 1,
  "message": "AddRaftVoterResponse",
  "api_key": 80,
  "direction": "response",
  "valid_versions": [
    0,
    1
  ],
  "flexible_versions": [
    0,
    1
  ],
  "source": "AddRaftVoterResponse.json",
  "cases": [
    {
      "name": "success",
      "why": "The ordinary success response in a fully flexible message.",
      "versions": [
        0,
        1
      ],
      "json_value": {
        "throttleTimeMs": 0,
        "errorCode": 0,
        "errorMessage": null
      }
    },
    {
      "name": "error_with_message",
      "why": "A nullable string carrying a value, distinct from the null case above.",
      "versions": [
        0,
        1
      ],
      "json_value": {
        "throttleTimeMs": 0,
        "errorCode": 81,
        "errorMessage": "voter already present"
      }
    },
    {
      "name": "synthesized_populated_v0",
      "why": "Every field this version declares, present and non-default: one element in every array, every nullable thing supplied. The defaults case leaves arrays empty, which hides every field of the struct the array would have held \u2014 a records field encoded with the wrong length prefix survived the whole corpus that way, because nothing ever reached the struct holding it.",
      "versions": [
        0
      ],
      "json_value": {
        "throttleTimeMs": 1,
        "errorCode": 1,
        "errorMessage": "v"
      }
    },
    {
      "name": "synthesized_populated_v1",
      "why": "Every field this version declares, present and non-default: one element in every array, every nullable thing supplied. The defaults case leaves arrays empty, which hides every field of the struct the array would have held \u2014 a records field encoded with the wrong length prefix survived the whole corpus that way, because nothing ever reached the struct holding it.",
      "versions": [
        1
      ],
      "json_value": {
        "throttleTimeMs": 1,
        "errorCode": 1,
        "errorMessage": "v"
      }
    }
  ]
}
